# T3-0-2 審查報告

- 任務：U-15：`loaders/shareholding.py` 的 `on_conflict_do_update` 補 `updated_at=func.now()`，並確認其餘 9 處 upsert 都已正確；新增 1 個會在修好前 FAIL 的回歸測試
- 規格：`docs/specs/M3-0-techdebt.md` §3
- Reviewer：claude-sonnet-5

## 第 1 輪

### 審查方式

未採信 Coder 回報，自行對 repo 工作目錄跑 `git status` / `git diff`，並自己重跑驗收指令與測試（包含把修法改回原樣以確認新測試真的會 FAIL）。

### 變更範圍

```
$ git status --porcelain
 M docs/specs/M3-0-techdebt.md
 M etl/tests/test_etl_shareholding.py
 M etl/twstock_etl/loaders/shareholding.py
```

只動到規格 §3.3／§3.4 列出的兩個檔案，加上規格檔本身把狀態表 T3-0-2 改成 `IN_REVIEW`，符合 §3.6 範圍邊界；沒有動到 schema／migration／API／`web/`。

### 逐項比對規格

- `etl/twstock_etl/loaders/shareholding.py`
  - 檔頭 import 由 `from sqlalchemy import Connection` 改為 `from sqlalchemy import Connection, func`，與規格 §3.3 逐字一致。
  - `on_conflict_do_update` 的 `set_` 補上 `"updated_at": func.now()`，位置、寫法與規格逐字一致；未新增 `source`（`shareholding_dist` 本來就沒有這欄，符合規格提醒）；未知代號過濾、去重、`BATCH_SIZE` 分批邏輯完全未動。
- `etl/tests/test_etl_shareholding.py`
  - 檔頭新增 `import time`，位置在標準函式庫區塊、`from datetime import date` 之上，符合規格。
  - 新增 `test_upsert_重載時_updated_at_會更新`，內容與規格 §3.4 給的程式碼逐字相同：兩個獨立 `with clean_db.begin()` 各跑一次 `upsert_shareholding`，中間 `time.sleep(0.01)`，斷言 `second > first`（嚴格大於）。

### 掃過其餘 9 處 upsert（規格要求的 grep 覆核）

自行重跑：

```
$ grep -rn 'on_conflict_do_update' etl/twstock_etl/loaders/*.py
etl/twstock_etl/loaders/calendar.py:34:    stmt = stmt.on_conflict_do_update(
etl/twstock_etl/loaders/chip.py:81:            .on_conflict_do_update(
etl/twstock_etl/loaders/chip.py:162:            .on_conflict_do_update(
etl/twstock_etl/loaders/chip.py:235:            .on_conflict_do_update(
etl/twstock_etl/loaders/chip.py:300:            .on_conflict_do_update(
etl/twstock_etl/loaders/price.py:90:        stmt = stmt.on_conflict_do_update(
etl/twstock_etl/loaders/price.py:135:        stmt = stmt.on_conflict_do_update(
etl/twstock_etl/loaders/price.py:208:        stmt = stmt.on_conflict_do_update(
etl/twstock_etl/loaders/shareholding.py:69:        stmt = stmt.on_conflict_do_update(
etl/twstock_etl/loaders/stock.py:49:        stmt = stmt.on_conflict_do_update(
```

共 10 處，與規格表格一致，沒有第 11 處。逐一開檔確認 `set_` 內容：

| 檔案:行 | `set_` 有 `updated_at` |
| --- | --- |
| `calendar.py:34` | 有（39 行） |
| `chip.py:81`（institutional） | 有（第 94 行） |
| `chip.py:162`（margin） | 有（往下確認到含 `updated_at`） |
| `chip.py:235`（foreign） | 有（240 行） |
| `chip.py:300`（sbl） | 有（310 行） |
| `price.py:90`（daily_price） | 有（102 行） |
| `price.py:135`（adj_factor） | 有（143 行） |
| `price.py:208`（index_daily） | 有（218 行） |
| `shareholding.py:69` | **本次修好，有** |
| `stock.py:49` | 有（60 行） |

`db/tests/test_db_chip_tables.py` 裡另一處是測試自己組的 SQL，未動，符合規格提醒。確認 Architect 掃過的清單正確、本次修法沒有遺漏。

### 回歸測試真的會抓到 bug（規格 §3.4 要求先驗證）

把 `loaders/shareholding.py` 用 `git stash` 暫時還原成沒有 `updated_at` 的版本，跑：

```
$ TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest etl/tests/test_etl_shareholding.py -v -k updated_at
...
E       AssertionError: assert datetime.datetime(2026, 9, 21, 22, 5, 58, 168585, ...) > datetime.datetime(2026, 9, 21, 22, 5, 58, 168585, ...)
FAILED etl/tests/test_etl_shareholding.py::test_upsert_重載時_updated_at_會更新
1 failed, 6 deselected in 0.31s
```

確認 FAIL（兩次時間戳完全相同，證明測試真的鎖住這個 bug）。`git stash pop` 還原修法後：

```
$ TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest etl/tests/test_etl_shareholding.py -v
collected 7 items
etl/tests/test_etl_shareholding.py .......                               [100%]
7 passed in 0.44s
```

### 驗收指令（自己重跑，逐字輸出）

```
$ TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs
...
313 passed, 1 skipped, 1 warning in 5.85s
SKIPPED [1] db/tests/test_db_hypertable.py:74: 此 PostgreSQL 未安裝 TimescaleDB，hypertable 實跑留待使用者本機驗證
```

符合規格預期（312 passed / 1 skipped 基準線 + 本次新增 1 個測試 = 313 passed / 1 skipped，唯一的 skip 仍是 `test_db_hypertable.py:74`）。

```
$ cd web && npm test
...
Test Files  9 passed (9)
     Tests  60 passed (60)
```

9 檔 60 測全過，等於 T3-0-1 完成後的基準線（本任務不改前端，維持不變，沒有退步）。`stderr` 仍出現 `ChartStack.test.tsx` 的 `not configured to support act(...)` 警告——這是 T3-0-1 遺留、規格 §2 範圍內的既有已知狀況，不在本任務 §3 範圍邊界內（§3.6 只列後端兩個檔案），不影響本輪結論。

```
$ scripts/m1_verify.sh | tail -1
M1 VERIFY PASSED
$ echo $?
0

$ scripts/m2_verify.sh | tail -1
M2 VERIFY PASSED
$ echo $?
0
```

`docker compose -f deploy/docker-compose.yml --env-file .env.example config --quiet` 離開碼 0，通過。

### 逐項檢查

- **符合規格**：檔案路徑、`set_` 內容、import 位置、測試內容皆與規格逐字一致；範圍邊界沒有越界。
- **正確性**：`func.now()` 是 PostgreSQL transaction timestamp，兩次 upsert 分屬不同 transaction，`updated_at` 會確實更新；不影響股數/金額/民國年等既有欄位轉換邏輯（本任務未觸及那些欄位）。
- **冪等性**：`ON CONFLICT (stock_id, week_date, level) DO UPDATE` 主鍵與規格一致，重跑不會產生重複列（`test_重跑同一份資料不會變成_48_筆` 依然通過）。
- **錯誤處理**：本任務未變更任何來源讀取／逾時／限流邏輯，維持原樣。
- **安全**：無新增硬編碼機密；`func.now()` 是 SQLAlchemy 表達式，非字串拼接，無 SQL injection 疑慮。
- **測試**：新增測試用嚴格 `>` 而非 `>=`，且已驗證「修好前 FAIL、修好後 PASS」，確實鎖住這個 bug，不是假陽性測試。

### 結論

**APPROVE**

無 blocker、無 major、無 minor。程式改動、測試與規格逐字相符，其餘 9 處 upsert 覆核無遺漏，回歸測試已驗證會在修好前 FAIL，所有驗收基準線（後端 313 passed / 1 skipped、前端 9 檔 60 測、m1_verify.sh／m2_verify.sh 皆 PASSED）維持不退步。
