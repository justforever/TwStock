# T1-1 審查報告：Migration 0002：價格三表 + etl_job_log，與 hypertable 雙簽名

## 第 1 輪

- 審查者：Reviewer（claude-sonnet-5）
- 日期：2026-09-19
- 規格：`docs/specs/M1-price.md` §T1-1（DDL 見 §3）
- Coder 回報 `changed_files`：`conftest.py`、`db/twstock_db/tables.py`、`db/twstock_db/timescale.py`、`db/migrations/versions/0002_price_tables.py`、`db/tests/test_db_hypertable.py`、`db/tests/test_db_migrations.py`、`docs/specs/M1-price.md`

### 審查方式

未採信 Coder 回報的驗收輸出，自行以 `git status` / `git diff` 檢視未 commit 變更，並重跑下列指令：

1. `TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs`
   → `97 passed, 1 skipped`（skip 為 `test_hypertables_registered_when_timescale_installed`，理由「此 PostgreSQL 未安裝 TimescaleDB」，與環境限制相符）。
2. `alembic downgrade base` → `alembic upgrade head` → `REVERSIBLE_OK`，log 顯示走「未安裝 TimescaleDB，維持一般資料表」路徑，與環境相符。
3. `psql \d daily_price / index_daily / adj_factor / etl_job_log`：逐欄型別、nullable、default、PK、索引、CHECK 約束皆與規格 §3 DDL **逐欄比對一致**；`etl_job_log_job_id_seq` 確認 `OWNED BY etl_job_log.job_id`（`pg_get_serial_sequence` 驗證），BIGSERIAL 行為正確。
4. `docker compose -f deploy/docker-compose.yml --env-file .env.example config --quiet` → 通過（本任務未動 compose 檔，僅確認未被波及）。
5. 確認 `db/twstock_db/tables.py` 未對 `daily_price` 加外鍵，符合 D-016；`timescale.py` 的雙簽名機制（`begin_nested()` SAVEPOINT + `DBAPIError` 捕捉退回舊簽名）與規格 §T1-1-2 逐字比對一致。
6. 確認本輪未觸及 `etl/` 任何 parser／loader 檔案，範圍與 T1-1（僅 schema/migration）相符；台股資料細節（還原股價累乘方向、除權息當日、民國年轉換、千分位逗號、空值符號、休市日、股 vs 張、ETL 冪等性）為 T1-2／T1-3 的職責範圍，本任務不涉及，故不適用。

### 發現

**Minor（不影響驗收，建議下一輪或 T1-2 前順手修正）**

- `db/tests/test_db_hypertable.py::test_create_hypertable_uses_by_range_first` 的斷言由規格要求的「`conn.execute` 被呼叫 1 次」（`==1`）弱化成 `assert conn.execute.call_count >= 1`。實測（見上）行為確實是呼叫 1 次，目前不影響測試結果，但 `>= 1` 無法偵測未來重構意外造成的多餘呼叫，建議之後改回 `== 1` 以完全符合規格斷言強度。

無 blocker、無 major 發現。四張表 DDL、雙簽名 hypertable 邏輯、migration 可逆性、CHECK 約束、BIGSERIAL sequence、conftest 的 `RESTART IDENTITY CASCADE` 均與規格一致且驗證通過。

### 結論

**APPROVE**

規格狀態表 T1-1 由 `IN_REVIEW` 改為 `DONE`。
