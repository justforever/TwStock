# T1-4b 審查報告

任務：`cli.py`：`load-index`／`load-exright` 的 skip 輸出對齊 §4 表格
規格：`docs/specs/M1-price.md`（T1-4b 小節）

## 第 1 輪

**審查方式**：不採信 Coder 回報，自行以 `git status` / `git diff` 檢視未 commit 變更，並實際跑驗收指令與測試（本機臨時 PostgreSQL 16，`scripts/pg_temp.sh`）。

### 變更範圍檢查

`git diff --stat`：

```
docs/specs/M1-price.md    |  2 +-
etl/tests/test_etl_cli.py | 40 ++++++++++++++++++++++++++++++++++++++--
etl/twstock_etl/cli.py    |  7 +++++--
```

- `cli.py` 只動了 `_cmd_load_index` 與 `_cmd_load_exright` 各一行 `print`，與規格「只准動這兩個檔案、各一行」的範圍完全一致，argparse、`_cmd_load_price`、錯誤處理均未變動。
- `test_etl_cli.py`：改寫 `test_load_exright_cli_skip` 最後兩行斷言為整行斷言、新增 `test_load_index_cli_skip`，程式碼與規格 §T1-4b 給出的範例逐字相符。
- `docs/specs/M1-price.md`：只改了狀態表 T1-4b 那一列（TODO → IN_REVIEW），符合「Coder 唯一可以改本文件的地方」。

### 自行執行驗收指令

1. `etl/tests/test_etl_cli.py` 單獨跑：13 passed（12 既有 + 1 新增），與規格預期一致。
2. 規格給的手動驗收指令（`scripts/pg_temp.sh reset` → migrate → `load-stocks` → 兩次 `load-exright`）：
   ```
   loaded exright from=2026-09-01 to=2026-09-30 rows=2
   skipped exright from=2026-09-01 to=2026-09-30 reason=已完成，略過
   ```
   逐字符合規格 §4 表格格式。
3. 額外手動測試 `load-index`（同一 fixture 連跑兩次）：因跑測當下（2026-09-19）落在當月，兩次都印 `loaded`——這是規格 D-026「當月一律視為未完成、不 skip」的既定行為，非本次改動的邏輯，不影響本任務。skip 分支已由新增的 `test_load_index_cli_skip`（塞入 2000-01 歷史成功紀錄，繞開當月限制）覆蓋，且該測試通過。
4. 全專案測試：`173 passed, 1 skipped in 2.08s`，與 Coder 回報一致（該 1 skipped 為既有的 TimescaleDB 未安裝略過測試，非本次新增）。

### 台股資料細節 / ETL 冪等性檢查

本任務僅修改 CLI 的 `print` 訊息文字，未觸及還原股價累乘方向、除權息計算、民國年轉換、千分位、空值符號、休市日判斷、股/張單位等領域邏輯（這些邏輯在 T1-2/T1-3/T1-4a 已處理，本次 diff 未觸碰對應檔案）。冪等性（skip 分支）行為本身也未變，只是輸出訊息更明確；以 `load_index_month`／`load_adj_factors` 原始碼確認 skip 判斷（`has_successful_run`）發生於解析 payload 之前，故新增測試用不匹配月份的 fixture 檔案不影響結果，符合規格註記。

### 結論

無 blocker、無 major 問題。範圍精確、程式碼與測試逐字符合規格、驗收指令與測試皆自行重跑通過。

**判定：APPROVE**

後續動作：規格狀態表 T1-4b 改為 `DONE`，並 commit。
