# T0-6 審查報告：Docker Compose、Dockerfile、M0 整合驗收腳本

## 第 1 輪

**審查者**：Reviewer
**規格**：`docs/specs/M0-skeleton.md` §T0-6

### 審查方式

- `git status` / `git diff` 檢視未 commit 變更，確認變更範圍僅為 `.dockerignore`、`api/Dockerfile`、`etl/Dockerfile`、`web/Dockerfile`、`web/nginx.conf`、`deploy/docker-compose.yml`（含刪除 `deploy/.gitkeep`）、`scripts/verify_search_all.py`、`scripts/m0_verify.sh`、`README.md`、`docs/specs/M0-skeleton.md`（狀態列），未觸及 T0-1～T0-5 的程式邏輯。
- 逐檔比對 `.dockerignore`、三個 `Dockerfile`、`web/nginx.conf`、`deploy/docker-compose.yml`、`scripts/verify_search_all.py`、`scripts/m0_verify.sh` 與規格逐字/逐條核對（全部與規格文字完全一致，無改動）。
- 自行執行驗收指令（不採信 Coder 回報的輸出）：
  1. `docker compose -f deploy/docker-compose.yml --env-file .env.example config --quiet` → 無輸出、exit 0，印出 `COMPOSE_OK`。
  2. `docker compose -f deploy/docker-compose.yml --env-file .env.example config --services | sort` → `api / db / etl / migrate / web`，與規格預期一致。
  3. `docker compose ... config`（完整輸出）檢查 anchor `*db_url` 展開、`depends_on` 條件、port 綁定 `127.0.0.1` 皆正確解析。
  4. 事先確認 54329/54330/18000 port 皆無殘留監聽、無殘留 postgres/uvicorn 行程。
  5. 執行 `scripts/m0_verify.sh`：輸出依序為 `== migrate` → `== load fixtures`（`loaded market=TWSE records=9 deactivated=0` / `loaded market=TPEx records=8 deactivated=0` / `loaded year=2026 days=365 open=250 closed=115`）→ `== start api` → `== verify search` → `OK 17/17 stocks searchable by id and name` → `M0 VERIFY PASSED`，exit 0。
  6. 再執行一次 `scripts/m0_verify.sh` 驗證冪等性：仍以 `M0 VERIFY PASSED` 結尾，exit 0；結束後 `ss -ltnp` 確認 54330/18000 無殘留監聽（僅剩一個已 `<defunct>` 的 postgres 殭屍行程，非監聽狀態，不影響）。
  7. `TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest` → `92 passed, 1 warning`（回歸，與 T0-1～T0-4 既有測試數一致）。
  8. `cd web && npm test` → `Test Files 2 passed (2)`、`Tests 11 passed (11)`（回歸）。

### 核對結果

- **`.dockerignore`**：內容與規格逐字相符。
- **三個 Dockerfile**：`api/Dockerfile`、`etl/Dockerfile`、`web/Dockerfile` 內容與規格逐字相符，版本鎖定正確（`python:3.12-slim-bookworm`、`node:22-alpine`、`nginx:1.27-alpine`）。`web/nginx.conf` 逐字相符，`/api/` 反向代理到 `api:8000`，SPA fallback 正確。
- **`deploy/docker-compose.yml`**：五個 service（db/migrate/api/etl/web）內容與規格逐字相符；`x-db-url` anchor 正確被 `migrate`/`api`/`etl` 三處引用；`depends_on` 依賴鏈（`db healthy` → `migrate` → `api`/`etl` completed_successfully；`web` depends_on `api`）正確；`migrate` 使用 `etl/Dockerfile` 建置映像但改用 `alembic` 指令，因 `twstock-etl` 依賴 `twstock-db`（含 `alembic==1.16.5`）而可用，經實測確認可行；port 皆綁定 `127.0.0.1`，未暴露對外。
- **`scripts/verify_search_all.py`**：參數、`parse_isin_html` 呼叫、逐筆代號/名稱搜尋驗證邏輯、失敗訊息格式、成功/失敗輸出與 exit code 皆與規格相符，實測 17/17 全部可搜尋。
- **`scripts/m0_verify.sh`**：`ROOT` 計算、獨立臨時叢集（`TWSTOCK_PGDATA=/tmp/twstock-pg-verify`、`TWSTOCK_PGPORT=54330`）、`trap cleanup EXIT`（kill uvicorn + `pg_temp.sh stop`）、`migrate` → `load fixtures`（三個 fixture 依序）→ `start api`（背景啟動、輪詢 `/api/health` 最多 40 次 × 0.5 秒）→ `verify search` → `M0 VERIFY PASSED` 的步驟順序與規格一致；可重複執行、結束後無殘留監聽 port，符合冪等性要求。
- **README「快速開始」**：涵蓋 Docker 部署、本機開發（無 Docker）、測試三個段落，內容與規格 §5 要求一致；「目前進度」那一行未被改動，符合「不要做的事」規範。
- **回歸測試**：後端 92 個測試全數通過、前端 11 個測試全數通過，未發現因本任務改動而破壞既有功能。
- **未做不該做的事**：未執行 `docker compose up`/`docker build`/`docker pull`；未寫 Helm/Redis/Nginx TLS；未 commit `.env` 或真實密碼（`.env` 已在 `.gitignore`，且 repo 中只有 `.env.example`）；未修改 T0-1～T0-5 程式邏輯。

### 觀察到的小瑕疵（非 blocker，記錄供參考）

1. **未追蹤的 `*.egg-info/` 目錄**：`api/twstock_api.egg-info/`、`db/twstock_db.egg-info/`、`etl/twstock_etl.egg-info/` 目前為未追蹤狀態（由先前任務 `pip install -e` 產生，非本任務新增，`git status` 顯示其建立時間早於本次 `m0_verify.sh` 執行）。這些目錄未列在 `.gitignore` 中，理論上有被誤 `git add -A` 帶入 commit 的風險。本次審查 commit 時將以明確檔案清單 `git add` 排除這些目錄，不影響本任務結論；建議後續任務將 `**/*.egg-info/` 加入 `.gitignore`。

以上僅為環境整潔面的觀察，不構成 blocker 或 major 問題，不要求本輪修改。

### 結論

**APPROVE**

規格狀態表已將 T0-6 改為 `DONE`，並建立本次 commit（僅加入本任務相關檔案，未包含未追蹤的 `egg-info/` 目錄）。
