# T0-1 審查報告

## 第 1 輪

- 審查者：Reviewer（claude-sonnet-5）
- 日期：2026-09-18
- 規格：`docs/specs/M0-skeleton.md` §T0-1
- Coder 回報 `changed_files`：`.env.example`、`conftest.py`、`pytest.ini`、`requirements-dev.txt`、`scripts/pg_temp.sh`、`db/pyproject.toml`、`db/alembic.ini`、`db/migrations/{env.py,script.py.mako,versions/0001_stock_trading_calendar.py}`、`db/twstock_db/{__init__.py,config.py,engine.py,tables.py,timescale.py,testing.py}`、`db/tests/{test_db_migrations.py,test_db_timescale.py}`、`docs/specs/M0-skeleton.md`

### 結論：**APPROVE**

### 驗證方式

未採信 Coder 回報，自行在本環境重新執行全部 7 條驗收指令：

1. `python3 -m venv .venv && .venv/bin/pip install -q -r requirements-dev.txt` → `INSTALL_OK`。
2. `scripts/pg_temp.sh reset && scripts/pg_temp.sh start` → 兩行皆為 `postgresql+psycopg://twstock:twstock@127.0.0.1:54329/twstock_test`；另外驗證 `start`（重複執行）、`stop`（重複執行）皆冪等、exit code 0。
3. `alembic -c db/alembic.ini upgrade head` → stderr 含 `Running upgrade  -> 0001, create stock and trading_calendar` 與 `TimescaleDB 不可用，維持一般資料表`（繁中 INFO log）。
4. `psql ... -c '\d stock' -c '\d trading_calendar'` → 欄位、型別、nullable、預設值、`ck_stock_market`、`ix_stock_market` 與規格 §7 DDL **逐項核對一致**。
5. `alembic downgrade base && alembic upgrade head` → `REVERSIBLE_OK`。
6. `TWSTOCK_TEST_DATABASE_URL=... pytest db/tests -rs` → `9 passed`（本環境無 TimescaleDB，`test_hypertable_noop_without_timescale` 為 passed 而非 skipped，符合預期）。
7. 未設 `TWSTOCK_TEST_DATABASE_URL` 時 `pytest db/tests -rs` → `1 passed, 8 skipped`，`test_get_database_url_missing` 仍 passed。

全部與規格預期輸出一致。

### 逐項檢查

- **符合規格**：檔案路徑、`twstock_db` 模組介面（`config.py`/`engine.py`/`tables.py`/`timescale.py`/`testing.py`）、Alembic 設定與 migration 內容與規格逐字比對，皆一致；`tables.py` 只用 SQLAlchemy Core，未寫 ORM class；migration 未使用 autogenerate（`target_metadata = None`）。
- **正確性**：`stock`/`trading_calendar` 兩表的欄位、型別、CHECK 約束、索引、預設值皆與 §7 DDL 相符（已用 `psql \d` 實測核對）。`create_hypertable_if_available` 對 `table`/`time_column` 先做識別字正則驗證，且在檢查擴充**之前**驗證（符合規格順序），SQL 使用 `CAST(:table AS regclass)` 等具名參數，未字串拼接。
- **冪等性**：`pg_temp.sh start`/`stop` 均可重複執行不報錯；migration downgrade→upgrade 可逆已驗證。
- **錯誤處理**：`get_database_url()`、`create_hypertable_if_available()` 之例外行為與規格一致（`RuntimeError`/`ValueError`）。
- **安全**：無硬編碼正式機密（`.env.example` 為範例值；`pg_temp.sh` 的 `twstock/twstock` 為規格明確允許的本機臨時測試帳密）；所有 SQL 皆用具名參數或 `text()` 綁定，無 f-string/`%` 拼接使用者輸入。
- **測試**：`db/tests/test_db_migrations.py`、`test_db_timescale.py` 涵蓋規格列出的全部 5＋4 個案例（表存在、欄位一致、CHECK 約束、預設值、可逆性；無效識別字、可用性一致、無 TimescaleDB 時 noop、缺少 `DATABASE_URL`）。測試檔名在 repo 內唯一，`db/tests` 下無 `__init__.py`，符合慣例。
- **狀態表**：規格底部狀態表已由 Coder 更新為 `IN_REVIEW`（Coder 回報聲稱如此，且 `git diff` 確認僅改動該一行，其餘規格文字未被更動）。

### 發現的問題

| 嚴重度 | 位置 | 問題 | 建議 |
| --- | --- | --- | --- |
| minor | `git diff --summary`：`api/.gitkeep`、`etl/.gitkeep`、`scripts/.gitkeep` 被刪除 | T0-1 規格的「新增/修改檔案」表只列出刪除 `db/migrations/.gitkeep`；規格明文「不要動 `etl/`、`api/`、`web/`、`deploy/`」。本輪 Coder 額外刪除了 `api/.gitkeep`、`etl/.gitkeep`、`scripts/.gitkeep`（`scripts/.gitkeep` 刪除尚可理解，因 `scripts/` 已有 `pg_temp.sh`）。經核對，這三個檔案本來就分別排定在 T0-2、T0-4、T0-6 刪除，此處提前刪除**未造成功能性影響**（空目錄本來就不會被 git 追蹤，之後任務新增檔案時行為不變），故不構成 blocker/major，但仍是超出本任務範圍的變更，提醒 Coder 之後任務嚴守「新增/修改檔案」表範圍，避免用會影響非本任務目錄的批次指令（例如 `find . -name .gitkeep -delete`）。 |
| minor | `db/twstock_db.egg-info/`（`pip install -e ./db` 產生） | 屬未加入 `.gitignore` 的建置產物，`git add -A` 會誤將其加入版本控制。本次審查不會把它加入 commit（改用明確路徑 `git add`），但建議下一輪或 T0-6 時把 `*.egg-info/` 加進根目錄 `.gitignore`。 |
| minor | `db/alembic.ini` `[logger_root] level` | 規格參考內容為 `level = WARNING`，Coder 實作為 `level = INFO`。不影響驗收指令輸出（驗收只檢查特定字串是否出現在 stderr），純屬與參考設定的微小落差，不影響功能。 |

以上三項均為 minor、不影響功能與驗收，故整體結論為 **APPROVE**。

### 後續動作

- 規格狀態表 T0-1 由 Reviewer 改為 `DONE`。
- 已提醒 Coder：後續任務刪檔案／改動檔案須嚴守規格「新增/修改檔案」表範圍。
