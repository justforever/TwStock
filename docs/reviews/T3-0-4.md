# T3-0-4 審查報告

- 任務：U-13：npm 弱點升級（`react-router-dom` 7.9.1 → 7.18.4、`vite` 7.1.5 → 7.3.6、`vitest` 3.2.4 → 3.2.7；只動 `web/package.json` 與 `web/package-lock.json`；獨立任務、獨立審查）
- 規格：`docs/specs/M3-0-techdebt.md` §5（`T3-0-4`）
- Reviewer：claude-sonnet-5

## 第 1 輪

### 審查方式

未採信 Coder 回報，自行對 repo 工作目錄跑 `git status` / `git diff`，並自己重跑規格完成標準列出的四條驗收指令。

### 變更範圍

```
$ git status
	modified:   docs/specs/M3-0-techdebt.md
	modified:   web/package-lock.json
	modified:   web/package.json
```

`web/package.json` 的 `git diff` 只動三個版本號：

```diff
-    "react-router-dom": "7.9.1"
+    "react-router-dom": "7.18.4"
...
-    "vite": "7.1.5",
-    "vitest": "3.2.4"
+    "vite": "7.3.6",
+    "vitest": "3.2.7"
```

`web/package-lock.json` 的變動全部是這三個套件升級所連帶牽動的相依樹（`@vitest/*` 系列 3.2.4→3.2.7、`@esbuild/*` 平台套件、`rollup`／`vite` 相關依賴等），未見任何與本任務無關的套件被手動改版本。`docs/specs/M3-0-techdebt.md` 的變動只是把狀態表 `T3-0-4` 改成 `IN_REVIEW`，符合共同規則。沒有動到 `package.json` 的 `scripts`／`dependencies` 其他欄位、沒有新增 Python 依賴、沒有動任何 `.py`／`.tsx`／`.ts` 程式檔，符合規格 §1.3「除 T3-0-4 外不要動 `web/package.json`」與「不要重構與該任務無關的程式」的邊界（本任務本身就是唯一允許改 `package.json` 版本號的任務）。

### 自行重跑驗收指令（規格完成標準 1～4，逐字輸出）

**1. 後端全測試**

```
$ TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs -q
.........s.............................................................. [ 22%]
........................................................................ [ 45%]
........................................................................ [ 68%]
........................................................................ [ 91%]
.......................... [100%]
=========================== short test summary info ============================
SKIPPED [1] db/tests/test_db_hypertable.py:74: 此 PostgreSQL 未安裝 TimescaleDB，hypertable 實跑留待使用者本機驗證
（exit code = 0；逐點計數 313 passed, 1 skipped）
```

與規格要求「313 passed, 1 skipped」一致（本任務不動任何後端程式碼與測試，數字理應與 T3-0-3 驗收時相同）。

**2. 前端測試**

```
$ cd web && npm test
 RUN  v3.2.7 /home/claude/TwStock/web
 ✓ src/pages/SearchPage.test.tsx (6 tests)
 ✓ src/pages/StockPage.test.tsx (9 tests)
 ✓ src/components/ChipTab.test.tsx (9 tests)
 ✓ src/components/ChartStack.test.tsx (11 tests)
 ✓ src/pages/EtlStatusPage.test.tsx (2 tests)
 ✓ src/chipMath.test.ts (8 tests)
 ✓ src/api.test.ts (5 tests)
 ✓ src/chartSync.test.ts (5 tests)
 ✓ src/ma.test.ts (5 tests)

 Test Files  9 passed (9)
      Tests  60 passed (60)
```

`vitest` 已由 3.2.4 換成 3.2.7（版本標頭可見）。stderr 僅出現 `StockPage.test.tsx` 故意觸發的 `Failed to fetch institutional data: Error: HTTP 500`（既有的預期內錯誤路徑輸出），另外自行確認：

```
$ npm test 2>&1 | grep -i 'act('
（無輸出）
```

`act(...)` 警告仍為 0，符合完成標準第 2 條。

**3. M1／M2 驗收腳本**

```
$ scripts/m1_verify.sh | tail -1
M1 VERIFY PASSED

$ scripts/m2_verify.sh | tail -1
M2 VERIFY PASSED
```

（`scripts/m2_verify.sh` 內含全部 10 項籌碼檢查與 9 項價格檢查，皆 OK）兩者離開碼皆 0。

**4. npm audit 與 build**

```
$ npm audit
# npm audit report

@vitest/mocker  2.1.0 - 4.1.10
Severity: moderate
Vitest: Path Traversal / Arbitrary File Read via @vitest/mocker Redirect Mock
fix available via `npm audit fix --force`
Will install vitest@5.0.1, which is a breaking change
node_modules/@vitest/mocker
  vitest  2.1.0-beta.1 - 4.1.10
  Depends on vulnerable versions of @vitest/mocker
  node_modules/vitest

2 moderate severity vulnerabilities
```

只剩 `@vitest/mocker` 與依賴它的 `vitest` 這 2 個 moderate，`critical`／`high` 均為 0，與規格「只剩 2 個 moderate」逐字相符（修法會裝 `vitest@5.0.1`，是規格明訂不做的破壞性升級）。

```
$ npm run build
> tsc -p tsconfig.json && vite build
vite v7.3.6 building client environment for production...
✓ 56 modules transformed.
✓ built in 1.94s
```

`vite` 版本標頭確認為 7.3.6，`tsc` 沒有報錯即代表 0 個 TypeScript error。

**docker compose 設定檔語法檢查**

```
$ docker compose -f deploy/docker-compose.yml --env-file .env.example config --quiet
(exit code = 0)
```

### 基準線比對

| 項目 | 基準線 | 本輪實測 |
| --- | --- | --- |
| 後端測試 | 312 passed / 1 skipped | 313 passed / 1 skipped（承接 T3-0-2 的新增測試，本任務不增減） |
| 前端測試 | 9 檔 57 測 | 9 檔 60 測（承接 T3-0-1 的新增測試，本任務不增減），act() 警告 0 次 |
| `m1_verify.sh` | PASSED | PASSED |
| `m2_verify.sh` | PASSED | PASSED |
| `npm audit` | — | critical 0／high 0／moderate 2（`@vitest/mocker`＋`vitest`），與規格預期一致 |
| `npm run build` | — | 0 個 TypeScript error |

四項基準線均未退步，且完成標準 1～4 全部逐字核對通過。

### 結論

`web/package.json` 與 `web/package-lock.json` 的改動範圍精確符合規格：只升級 `react-router-dom`／`vite`／`vitest` 三個套件版本號，鎖定版本（不帶 `^`），沒有動到任何程式邏輯、API 介面或資料庫；`package-lock.json` 的連帶變動全部可歸因於這三個套件的相依樹更新。四條完成標準逐一自行重跑驗證皆通過，基準線沒有退步。

**判定：APPROVE**（無 blocker、無 major）。
