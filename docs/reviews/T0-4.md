# T0-4 審查報告：API：FastAPI 個股搜尋與健康檢查

## 第 1 輪

**結論：APPROVE**

### 驗收過程（Reviewer 自行執行，未採信 Coder 回報）

1. `git status` / `git diff`：確認未 commit 變更僅為 `docs/specs/M0-skeleton.md`（狀態欄）、`requirements-dev.txt`（追加 `-e ./api`），加上新增未追蹤的 `api/` 目錄。
2. `.venv/bin/pip install -q -r requirements-dev.txt` 安裝成功。
3. `scripts/pg_temp.sh start` 起臨時 PostgreSQL，輸出連線字串正常。
4. `TWSTOCK_TEST_DATABASE_URL=... .venv/bin/python -m pytest -rs`：**92 passed, 1 warning**（db/etl/api 全部通過，0 skipped），與規格預期一致。
5. 整合驗收：
   - `alembic upgrade head` 成功。
   - `twstock_etl.cli load-stocks --market TWSE ...` → `loaded market=TWSE records=9 deactivated=0`，與規格預期完全相符。
   - 啟動 uvicorn，`curl /api/health` → `{"status":"ok","db":"ok"}`。
   - `curl /api/stocks?q=臺積` → `{"query":"臺積","count":1,"items":[{"stock_id":"2330","name":"台積電","market":"TWSE","industry":"半導體業","listed_date":"1994-09-05","is_etf":false}]}`，逐字元與規格範例相符。
   - 額外自行補測：`q=  `（全空白）→ 422；`q=x!y`（含跳脫字元 `!`）→ 200 且無 SQL 錯誤、`count=0`，確認 LIKE 跳脫邏輯正確、無例外洩漏。

### 逐項檢查

- **符合規格**：檔案路徑、模組介面（`schemas.py`、`repository.py`、`deps.py`、`routers/stocks.py`、`routers/health.py`、`main.py`）與規格逐字一致；SQL 語句照規格參數化寫法照抄，未做字串拼接。
- **正確性**：
  - `escape_like` 先處理 `!` 再處理 `%`、`_`，順序正確，跳脫不會互相污染。
  - `normalize_query` 先 `strip()` 再做臺→台替換，符合規格。
  - 排序 CASE 邏輯（完全相符 → 代號前綴 → 名稱前綴 → 名稱包含，同級依 `length(stock_id)`、`stock_id`）與規格 SQL 一致。
  - `routers/stocks.py` 用同步 `def`（非 async），透過 `Depends(get_db_engine)` + `with engine.connect()`，符合規格「不要用 async SQLAlchemy」的限制。
  - `/api/health` 失敗時回 503 且用 `JSONResponse` 直接輸出固定訊息，未把例外訊息回傳給前端；`logger.exception` 有記錄。
- **安全**：SQL 全部走 `text()` + bound parameters，無字串拼接帶入使用者輸入；無硬編碼機密。
- **測試**：`test_api_stocks.py` 涵蓋規格表列全部 15 個案例（完全相符、代號前綴、名稱包含、臺台互通兩例、大小寫不敏感、名稱前綴排序、名稱包含但代號不符、已下市過濾、`%` 跳脫、`limit`、缺 `q`、空白 `q`、`limit` 邊界、`q` 超長），`test_api_repository.py`、`test_api_health.py` 亦涵蓋規格要求案例。經 Reviewer 實際執行全數通過。
- **未做規格外的事**：未加 CORS/認證/快取/pg_trgm，未寫 `/api/stocks/{id}`，符合「不要做的事」清單。

### 問題清單

無 blocker / major / minor 問題。

### 後續動作

規格狀態表 T0-4 已改為 `DONE`，並提交本次審查對應的 commit。
