# T3-0-3 審查報告

- 任務：U-16：三項風格債（`etl/twstock_etl/jobs.py` 函式內 import 移到模組頂層、`web/src/setupTests.ts` 補 `IS_REACT_ACT_ENVIRONMENT` 旗標消除 `act()` 警告、`api/twstock_api/chip_repository.py` `_LATEST_DATE_SQL` 改成 `dict[str, TextClause]`）——三處皆行為等價，只改寫法
- 規格：`docs/specs/M3-0-techdebt.md` §4
- Reviewer：claude-sonnet-5

## 第 1 輪

### 審查方式

未採信 Coder 回報，自行對 repo 工作目錄跑 `git status` / `git diff`，並自己重跑規格 §4.4 列出的全部驗收指令與測試。

### 變更範圍

```
$ git status
	modified:   api/twstock_api/chip_repository.py
	modified:   docs/specs/M3-0-techdebt.md
	modified:   etl/twstock_etl/jobs.py
	modified:   web/src/setupTests.ts
```

只動到規格 §4.1／§4.2／§4.3 列出的三個檔案，加上規格檔本身把狀態表 T3-0-3 改成 `IN_REVIEW`，符合 §4.5 範圍邊界；沒有動到 schema、API 回應格式、`ChipTab.tsx`／`chartSync.ts`／`StockPage.tsx`、`web/package.json`，也沒有動到 `chip_repository.py` 裡其他 `text(...)` 查詢或 `price_repository.py`。

### 逐項比對規格

#### 4.1 `etl/twstock_etl/jobs.py`：函式內 import 移到頂層

`git diff` 顯示 `load_shareholding()` 內原本的兩行函式內 import 已刪除，改放到模組頂層：
- `from twstock_etl.loaders.shareholding import upsert_shareholding` 插在 `from twstock_etl.loaders.price import (...)` 之後、`from twstock_etl.loaders.stock import (...)` 之前，逐字符合規格。
- `from twstock_etl.sources.tdcc import fetch_tdcc_shareholding, parse_tdcc_shareholding` 插在 `from twstock_etl.sources.isin import ...` 之後、`from twstock_etl.sources.tpex_price import ...` 之前，逐字符合規格。

自行重跑規格要求的兩個驗證指令：

```
$ .venv/bin/python -c "import twstock_etl.jobs; import twstock_etl.cli; import twstock_etl.scheduler; print('IMPORT_OK')"
IMPORT_OK

$ grep -n '^\s\+\(from\|import\) ' etl/twstock_etl/jobs.py
(無輸出，exit code = 1)
```

均與規格預期一致，模組內已無其他函式內 import，且無循環 import 問題。

#### 4.2 `web/src/setupTests.ts`：關掉 `act(...)` 提示

`git diff` 顯示檔尾新增：

```ts
// React 19 需要這個旗標才認得測試環境的 act()，否則每次 render 都會印警告
;(globalThis as unknown as { IS_REACT_ACT_ENVIRONMENT: boolean }).IS_REACT_ACT_ENVIRONMENT = true
```

與規格逐字一致，未改動任何測試檔、`vite.config.ts`，也沒有加 `@testing-library` 設定檔。

#### 4.3 `api/twstock_api/chip_repository.py`：`_LATEST_DATE_SQL` 型別註記

`git diff` 顯示：
- 檔頭新增 `from sqlalchemy.sql.elements import TextClause`（`text` 本已 import，未重複）。
- `_LATEST_DATE_SQL` 型別由 `dict[str, str]` 改為 `dict[str, TextClause]`，四個 value 都改成先呼叫 `text(...)` 包好，SQL 字串一字未改，仍是參數化的 `:stock_id`。
- `latest_chip_date()` 內 `sql_str = _LATEST_DATE_SQL[kind]` / `conn.execute(text(sql_str), ...)` 改為 `stmt = _LATEST_DATE_SQL[kind]` / `conn.execute(stmt, ...)`，沒有重複包一次 `text()`。
- `if kind not in _LATEST_DATE_SQL: raise ValueError(...)` 一字未動，錯誤訊息維持 `f"不支援的籌碼種類：{kind}"`。

逐字比對規格 §4.3，完全一致。

### 自行重跑驗收指令（規格 §4.4，逐字輸出）

**後端全測試**

```
$ TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs
...
SKIPPED [1] db/tests/test_db_hypertable.py:74: 此 PostgreSQL 未安裝 TimescaleDB，hypertable 實跑留待使用者本機驗證
313 passed, 1 skipped, 1 warning in 5.70s
```

313 passed / 1 skipped，與規格「T3-0-2 已完成的前提下」預期一致，本任務未新增也未刪除任何測試。

**前端測試**

```
$ cd web && npm test
...
 Test Files  9 passed (9)
      Tests  60 passed (60)
```

stderr 中僅出現 `StockPage.test.tsx` 故意觸發的 `Failed to fetch institutional data: Error: HTTP 500`（規格 §4.2 註明這是預期內、不必清除的錯誤路徑輸出）。另外自行統計：

```
$ npm test 2>&1 | grep -c 'not configured to support act'
0
```

`act(...)` 警告已歸零，符合規格預期。

**前端 build**

```
$ npm run build
✓ 56 modules transformed.
✓ built in 1.97s
```

0 個 TypeScript error。

**M1／M2 驗收腳本**

```
$ scripts/m1_verify.sh | tail -1
M1 VERIFY PASSED

$ scripts/m2_verify.sh | tail -1
M2 VERIFY PASSED
```

兩者離開碼皆 0（`jobs.py` 與 `chip_repository.py` 都在這兩條路徑上）。

**docker compose 設定檔語法檢查**

```
$ docker compose -f deploy/docker-compose.yml --env-file .env.example config --quiet
(exit code = 0)
```

### 基準線比對

| 項目 | 基準線 | 本輪實測 |
| --- | --- | --- |
| 後端測試 | 312 passed / 1 skipped | 313 passed / 1 skipped（含 T3-0-2 新增的 1 個測試，本任務不增減） |
| 前端測試 | 9 檔 57 測 | 9 檔 60 測（含 T3-0-1 新增的 3 個測試，本任務不增減），act() 警告 0 次 |
| `m1_verify.sh` | PASSED | PASSED |
| `m2_verify.sh` | PASSED | PASSED |

四項基準線均未退步。

### 結論

三處改動都逐字符合規格、行為等價（未改任何邏輯、錯誤訊息、SQL 文字或函式簽名），測試數量沒有變化，所有驗收指令自行重跑皆與規格預期相符，基準線沒有退步。

**判定：APPROVE**（無 blocker、無 major）。
