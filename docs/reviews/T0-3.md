# T0-3 審查報告：ETL loader、job、CLI、排程

## 第 1 輪

- 審查者：Reviewer（claude-sonnet-5）
- 日期：2026-09-18
- 規格：`docs/specs/M0-skeleton.md` §T0-3
- Coder 回報：見任務訊息（`acceptance_output`）

### 審查方式

自行執行（未採信 Coder 回報）：

```bash
cd /home/claude/TwStock && git status --porcelain=v1   # 只有 docs/specs/M0-skeleton.md 狀態列與新檔案，無其他既有檔案被改動
cd /home/claude/TwStock && scripts/pg_temp.sh start
cd /home/claude/TwStock && TWSTOCK_TEST_DATABASE_URL=postgresql+psycopg://twstock:twstock@127.0.0.1:54329/twstock_test .venv/bin/python -m pytest db/tests etl/tests -rs
cd /home/claude/TwStock && export DATABASE_URL=... && alembic upgrade head && cli load-stocks/load-calendar ... && psql 驗證
```

### 結果

- `pytest db/tests etl/tests`：**69 passed, 0 skipped**（與規格要求一致）。
- CLI 驗收流程輸出與規格逐行相符：
  ```
  loaded market=TWSE records=9 deactivated=0
  loaded market=TPEx records=8 deactivated=0
  loaded market=TPEx records=8 deactivated=0   # 第二次載入證明冪等
  loaded year=2026 days=365 open=250 closed=115
  TPEx|8|2
  TWSE|9|3
  ```
- 額外手動驗證：
  - log 走 stderr（`2>/dev/null` 後 stdout 只剩一行 `loaded ...`）——符合規格。
  - `--deactivate-missing` 對 9 筆 TWSE fixture 觸發筆數保護：`error: TWSE 個股僅 9 筆，少於 500，拒絕停用缺漏個股`，exit=1。
  - 未設定 `DATABASE_URL` 時：`error: 環境變數 DATABASE_URL 未設定`，exit=1，且訊息含 `DATABASE_URL`。

### 逐項檢查

1. **符合規格**：
   - 檔案路徑、函式簽名（`upsert_stocks`、`deactivate_missing`、`upsert_calendar`、`refresh_stock_list`、`refresh_trading_calendar`、`build_scheduler` 等）均與規格介面一致。
   - `upsert_stocks` 使用 `postgresql.insert` + `on_conflict_do_update(index_elements=["stock_id"])`，更新欄位集合與規格相符（不含 `created_at`），空 records 直接 return 0，未發 SQL——正確。
   - `deactivate_missing` 用 Core `not_in`、`market`/`is_active` 條件與規格 SQL 語意一致；`keep_ids` 空 → `ValueError("keep_ids 不可為空")`——正確。
   - `refresh_stock_list` 在寫 DB **之前**檢查筆數保護（`test_refresh_stock_list_deactivate_guard` 驗證 DB 內 0 列）——正確。
   - `build_scheduler` 的兩個 job 的 trigger、`args`、`coalesce`、`max_instances`、`misfire_grace_time`、`next_run_time` 皆依規格設定；`scheduler.py` 以 `from twstock_etl.jobs import refresh_stock_list, refresh_trading_calendar` 具名匯入，符合「monkeypatch `twstock_etl.scheduler.refresh_stock_list`」的測試需求。
   - `cli.py` 用 argparse、`main(argv=None)`、`if __name__ == "__main__": raise SystemExit(main())`、三個子指令參數與 stdout 格式皆符合規格表格。
   - 兩個 loader 函式皆不自行 commit，交易由 `jobs.py` 的 `engine.begin()` 控制——正確。
2. **正確性（台股細節）**：本任務不涉及新的台股格式解析（沿用 T0-2 parser），loader 邏輯無資料轉換需求，未發現問題。
3. **冪等性**：`test_upsert_stocks_idempotent`、`test_upsert_calendar_idempotent`、CLI 驗收指令中第二次載入 TPEx（筆數不變）皆驗證通過。
4. **錯誤處理**：`SourceFormatError`、`RuntimeError`（DATABASE_URL 未設定）、`FileNotFoundError`（不存在的檔案）、筆數保護皆有測試覆蓋且手動重跑通過；`run_stock_list_job` 對單一市場失敗只記 log、不中斷另一市場，測試以 monkeypatch 驗證呼叫兩次。
5. **安全**：SQL 全部走 SQLAlchemy Core／`postgresql.insert`，無 f-string 拼接使用者輸入；無硬編碼機密。
6. **測試覆蓋**：`test_etl_loaders.py`（9 個）、`test_etl_cli.py`（7 個）、`test_etl_scheduler.py`（3 個）皆存在且案例與規格§6 逐條對應，全部通過；測試目錄無 `__init__.py`，檔名全 repo 唯一。

### 問題清單

未發現 blocker 或 major 問題。以下為 minor（不影響功能與驗收，建議下次任務或小修順手清掉即可，不要求本輪回頭修改）：

| 嚴重度 | 檔案:行號 | 問題 | 建議 |
| --- | --- | --- | --- |
| minor | `etl/twstock_etl/loaders/stock.py:5,7` | `typing.Any`、`sqlalchemy.insert`、`sqlalchemy.text` 匯入但未使用 | 移除未用匯入 |
| minor | `etl/twstock_etl/jobs.py:12` | `CalendarDay`、`StockRecord` 匯入但未使用 | 移除未用匯入 |
| minor | `etl/twstock_etl/cli.py:13` | `get_database_url` 匯入但未使用（實際靠 `get_engine()` 內部呼叫觸發 `RuntimeError`） | 移除未用匯入 |
| minor | `etl/tests/test_etl_loaders.py`、`test_etl_cli.py` | 數個未使用的匯入（`get_engine`、`refresh_trading_calendar`、`CalendarDay`、`json`、`create_engine`、`func`、`select`、`stock`） | 順手清理，不影響測試正確性 |
| minor | `.gitignore` | 未列 `*.egg-info`（`db/twstock_db.egg-info/`、`etl/twstock_etl.egg-info/` 目前為未追蹤檔案），非本任務新增問題，但建議之後任一任務補上以免誤 commit | 加入 `.gitignore` |

### 結論

**APPROVE**——實作與測試皆符合規格，驗收指令輸出與規格範例逐字相符，未採信回報、獨立重跑全部通過。僅有上述數個未使用匯入等 minor 風格問題，不影響功能、冪等性或正確性，不要求本輪修正。

已將規格狀態表 T0-3 改為 `DONE` 並提交。
