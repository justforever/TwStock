# T2-2 審查報告：Migration 0004：籌碼四表與 `models.py` 五個 dataclass

規格：`docs/specs/M2-chips.md` §3、T2-2 章節。

## 第 1 輪

### 變更範圍

```
db/twstock_db/tables.py                       改：加 4 張表
db/migrations/versions/0004_chip_tables.py    新增
etl/twstock_etl/models.py                     改：加 5 個 dataclass
db/tests/test_db_chip_tables.py               新增
docs/specs/M2-chips.md                        改：T2-2 狀態表 TODO → IN_REVIEW（追蹤用，非規格內容）
```

符合規格「只准動上面 4 個檔案」的限制（狀態表更新為任務追蹤慣例，非程式碼）。未動 M1 的四張表、`0002_price_tables.py`，`models.py` 既有六個 dataclass 未被更動，未加任何外鍵，未寫 parser / loader / job / CLI / API / 前端。

### 自行驗證（不採信回報）

1. `db/twstock_db/tables.py`：四張表 `institutional_daily`、`margin_daily`、`foreign_holding`、`shareholding_dist` 逐欄比對 §3 DDL，型別對照（`String`/`Date`/`BigInteger`/`SmallInteger`/`Integer`/`Numeric(8,4)`/`DateTime(timezone=True)`）、`server_default=text("0")`、`CheckConstraint` 名稱皆與規格一致，風格與既有 `daily_price` 一致。

2. `db/migrations/versions/0004_chip_tables.py`：
   - `revision="0004"`、`down_revision="0003"` 正確。
   - `upgrade()` 依 §3 順序建表、建索引，最後呼叫四次 `create_hypertable_if_available`，寫法與 `0002_price_tables.py` 一致；無 `BIGSERIAL`/sequence（正確，四表主鍵皆非自增）。
   - `downgrade()` 反序 `drop_table`：`shareholding_dist` → `foreign_holding` → `margin_daily` → `institutional_daily`，與規格一致。

3. `etl/twstock_etl/models.py`：五個 `@dataclass(frozen=True)`（`InstitutionalRecord`、`MarginRecord`、`SblRecord`、`ForeignHoldingRecord`、`ShareholdingRecord`）欄位、型別、預設值（`source: str = "TWSE"` 等）與規格逐字一致。

4. **實跑驗收指令**（環境：本機 PostgreSQL 16，臨時叢集 `scripts/pg_temp.sh`）：

   ```bash
   TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs
   # 228 passed, 1 skipped（TimescaleDB 那筆），符合預期
   ```

   ```bash
   DATABASE_URL=$(TWSTOCK_PGDATA=/tmp/twstock-pg-t22b TWSTOCK_PGPORT=54399 scripts/pg_temp.sh reset) \
     .venv/bin/alembic -c db/alembic.ini upgrade head && \
     .venv/bin/alembic -c db/alembic.ini downgrade base && \
     .venv/bin/alembic -c db/alembic.ini upgrade head && echo REVERSIBLE_OK
   # 輸出 REVERSIBLE_OK；升級/降級/再升級各出現對應「未安裝 TimescaleDB，維持一般資料表」訊息，符合預期
   ```

   ```bash
   psql "postgresql://twstock:twstock@127.0.0.1:54399/twstock_test" -c '\d shareholding_dist'
   # 欄位、PK (stock_id, week_date, level)、ck_shareholding_dist_level、ix_shareholding_dist_week 與 §3 DDL 逐欄一致
   ```

   ```bash
   scripts/m1_verify.sh | tail -1
   # M1 VERIFY PASSED（回歸通過）
   ```

   另補跑（工作環境限制要求）：
   ```bash
   docker compose -f deploy/docker-compose.yml --env-file .env.example config --quiet
   # 通過，無輸出
   ```

5. `db/tests/test_db_chip_tables.py` 六項測試逐一核對規格第 4 節列出的六個案例（四張表存在、`institutional_daily` 欄位型別、`shareholding_dist` 三欄主鍵、`level` CHECK 擋 0/18 放行 17、`margin_daily.source` CHECK、`institutional_daily` upsert 覆蓋且列數為 1），案例齊全且各自用獨立 `engine.begin()` 區塊，符合規格對交易汙染的要求。

### 逐項檢查結論

- **符合規格**：檔案路徑、介面、欄位、範圍邊界皆符合，DDL 與規格逐欄一致。
- **正確性**：本任務不含台股資料細節（無民國年轉換、千分位、`--` 空值、股/張等問題，這些留待後續 parser 任務）；型別對照、CHECK 約束、預設值皆正確。
- **冪等性**：本任務未寫 ETL / loader，冪等性測試（`test_可以_upsert_同一個主鍵`）驗證了資料表層的 `ON CONFLICT DO UPDATE` 語意可用，符合 T2-2 範圍。
- **錯誤處理**：不適用（無來源存取）。
- **安全**：無硬編碼機密；測試中的 SQL 皆用具名參數綁定（`text(...)` + dict），無字串拼接。
- **測試**：涵蓋規格列出的六個邊界案例，且每個 CHECK 測試使用獨立交易區塊，符合要求。

未發現 blocker 或 major 問題。

### 結論：**APPROVE**

狀態表 T2-2 改為 DONE，本審查報告連結補上。
