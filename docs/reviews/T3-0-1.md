# T3-0-1 審查報告

- 任務：U-17 副圖資料換算成張（新增 `chipMath.lotsValue`，與 pane 標題和讀數面板一致；新增 3 個前端測試）
- 規格：`docs/specs/M3-0-techdebt.md` §2
- Reviewer：claude-sonnet-5

## 第 1 輪

### 審查方式

未採信 Coder 回報，自行對 repo 工作目錄跑 `git status` / `git diff`，並逐一重跑驗收指令。

### 變更範圍

```
$ git status --porcelain
 M docs/specs/M3-0-techdebt.md
 M web/src/chipMath.test.ts
 M web/src/chipMath.ts
 M web/src/components/ChartStack.test.tsx
 M web/src/components/ChartStack.tsx
```

只動到規格 §2.3 列出的 4 個檔案，加上規格檔本身補了 §9 任務狀態表；沒有動到 `ChipTab.tsx`、`chartSync.ts`、`StockPage.tsx`、任何後端檔案或 `web/package.json`，符合 §2.9 範圍邊界。

### 逐項比對規格

- `chipMath.ts`：新增 `lotsValue(shares: number): number`（`Math.round(shares / 1000)`），`toLots` 改成呼叫 `lotsValue`，`'—'` 邏輯留在 `toLots`，簽名與規格逐字一致。
- `chipMath.test.ts`：新增 `lotsValue 回傳整數張` 測試，斷言與規格逐字一致（20200000→20200、0→0、-5000000→-5000、1499→1）。
- `ChartStack.tsx`：import 改為 `lotsValue, toLots, toPercent`；7 處呼叫點（成交量 setData/valueByTime、法人 setData/valueByTime、融資 setData、融券 setData、valueByTimeMargin）全部套用 `lotsValue()`，與規格表格逐一比對一致；主圖 `valueByTimeMain`（`bar.close`）、讀數面板 9 個 `<span>`、pane 標題「（張）」字樣皆原樣未動。
- `ChartStack.test.tsx`：mock 補 `__series` 陣列並在三個 `add*Series` 推入，寫法與規格一致；新增的 2 個測試（setData 換算成張、十字線同步值與讀數面板一致）斷言數字與規格表格一致；既有 9 個測試逐一確認未被改動（含「讀數面板跟著十字線換日期」那個仍斷言 `5,000`／`19,500`）。

### 自行重跑的驗收指令與逐字輸出

**`npm test`**（`cd /home/claude/TwStock/web`）：

```
 ✓ src/components/ChartStack.test.tsx (11 tests) 129ms
 ✓ src/chipMath.test.ts (8 tests) 19ms
 Test Files  9 passed (9)
      Tests  60 passed (60)
```

與規格 §2.8 預期完全一致（基準線 9 檔 57 測 → 9 檔 60 測，新增 3 個測試：`lotsValue` 1 個 + `ChartStack` 2 個）。`act(...)` 警告仍出現 6 次，符合規格 §2.9「不要順手做 T3-0-3 的 setupTests.ts，這一輪還會出現屬預期」。

**`npm run build`**：

```
✓ 56 modules transformed.
dist/assets/index-BjxGMQp7.js   406.34 kB │ gzip: 129.67 kB
✓ built in 1.94s
```

0 個 TypeScript error，模組數與規格預期一致。

**後端基準線**（`TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs`）：

```
312 passed, 1 skipped, 1 warning
SKIPPED [1] db/tests/test_db_hypertable.py:74
```

本任務未動後端，與驗收基準線 312 passed / 1 skipped 一致，沒有退步。

**`scripts/m1_verify.sh`**：`M1 VERIFY PASSED`，離開碼 0。
**`scripts/m2_verify.sh`**：`M2 VERIFY PASSED`，離開碼 0。

**`docker compose -f deploy/docker-compose.yml --env-file .env.example config --quiet`**：離開碼 0，無錯誤輸出。

### 結論

程式改動與測試新增皆逐字符合規格 §2，未逾越範圍邊界，驗收基準線（後端 312/1 skip、前端由 9 檔 57 測提升為 9 檔 60 測、m1/m2 verify PASSED）沒有退步。無 blocker、無 major。

**結論：APPROVE**

已將 `docs/specs/M3-0-techdebt.md` §9 任務狀態表 T3-0-1 改為 `DONE`。
