# T0-2 審查報告：ETL 來源 parser（個股清單、TWSE 休市日）

## 第 1 輪

### 審查方式

未採信 Coder 回報，自行以獨立 venv（`/tmp/reviewvenv`）重跑驗收指令與測試，並逐一比對規格 `docs/specs/M0-skeleton.md` §T0-2 的介面定義、演算法規則、fixture 內容、測試案例清單。

### 執行結果

1. `pip install -q -r requirements-dev.txt` → `INSTALL_OK`
2. `python -m pytest etl/tests -rs`：

   ```
   etl/tests/test_etl_dates.py ............                                 [ 29%]
   etl/tests/test_etl_http.py ......                                        [ 43%]
   etl/tests/test_etl_isin.py ..............                                [ 78%]
   etl/tests/test_etl_twse_holiday.py .........                             [100%]
   41 passed in 0.14s
   ```

   全部通過，0 failed、0 skipped。

3. 手工驗證 ISIN parser：

   ```
   TWSE 9 3
   TPEx 8 2
   ```

   與規格預期輸出一致。

### 逐項比對

- **檔案清單**：`etl/pyproject.toml`、`errors.py`、`models.py`、`dates.py`、`http.py`、`sources/{__init__.py,isin.py,twse_holiday.py}`、四個測試檔、三個 fixture、`fixtures/README.md`、`requirements-dev.txt` 追加 `-e ./etl`，均與規格路徑一致；`etl/.gitkeep` 已不存在（規格要求刪除，符合）。
- **`etl/pyproject.toml`**：依賴版本與規格逐字一致。
- **`models.py` / `errors.py`**：欄位、型別、docstring 與規格一致。
- **`dates.py`**：西元 `YYYY-MM-DD`/`YYYY/MM/DD`、西元 8 位、民國 7 位、民國 6 位、民國 `RRR/MM/DD`、`RR/M/D` 全部規則正確實作；無效日期（`2026-02-30`）、空字串、`"--"`、`"abc"` 均正確 raise `ValueError`。測試以個別 `test_*` 函式（非 parametrize）覆蓋規格表格每一列，含成功與失敗案例，符合規格要求的覆蓋範圍。
- **`http.py`**：`default_client` 與 `get_with_retry` 行為（重試次數、`sleep(backoff_seconds * i)`、4xx 不重試、5xx/429 重試、`TransportError` 重試、最終失敗往外拋例外）與規格逐條核對皆正確；`test_etl_http.py` 六個測試案例（503→200、三次 503 皆失敗且驗證 sleep 呼叫序列 `[5.0, 10.0]`、404 不重試、402 不重試、連線錯誤重試後成功、User-Agent 驗證）皆對應規格 §4 六種情境，斷言內容（呼叫次數、sleep 參數）與規格描述一致。
- **`sources/isin.py`**：`ISIN_URLS`、`STOCK_SECTIONS`、`ETF_SECTIONS`、`_CODE_RE` 與規格一致；`decode_isin_bytes` 先 cp950 嚴格解碼、失敗才 `big5hkscs errors="replace"`，符合規格；`parse_isin_html` 演算法（區段列判斷、表頭跳過、全形空白優先/半形空白 fallback、代號正則過濾、重複代號去重保留首筆、`listed_date` 解析失敗僅設 `None` 不跳列、`industry`/`isin_code`/`cfi_code` 空字串轉 `None`、`is_etf` 依區段判斷、市場別以參數而非欄位判斷、結果為空 raise `SourceFormatError`）逐條核對皆正確實作。fixture 內容與規格 §7 逐字比對（`diff`）完全一致，代號順序與筆數（TWSE 9 筆 3 ETF、TPEx 8 筆 2 ETF）符合預期。測試涵蓋 fixture 解析、欄位正確性、ETF 判斷、非股票區段排除（`030001`/`2881A`/`9105` 不出現）、特殊符號名稱不被誤轉換（`矽力*-KY`、`臺企銀`）、空結果 raise、非法 market raise、壞列跳過、上市日期解析失敗保留該列、半形空白 fallback、cp950 編碼路徑，對應規格 §8 全部 12 項測試案例。
- **`sources/twse_holiday.py`**：`fetch_holiday_schedule` 非 list 回應正確 raise `SourceFormatError`；`parse_holiday_schedule` 缺 `Name`/`Date` 鍵、日期解析失敗皆正確轉為 `SourceFormatError`，`Description` 缺省為 `""`，`is_trading_day` 判斷邏輯（`TRADING_DAY_KEYWORDS` 出現在 `Name` 或 `Description`）正確；`build_calendar` 年份過濾、無資料 raise、假日優先於週末判斷、`note` 截斷至 64 字、輸出排序與天數（365/366）皆正確。fixture JSON 與規格逐字比對一致；2026 年計算結果開市 250、休市 115 與規格預期相符，並以獨立測試驗證 `2026-01-01`（休市/國定假日 note）、`2026-01-02`（開市/note=None，即使名稱含「開始交易日」）、`2026-01-03`（休市/週末）、`2026-02-11`（開市）、`2026-02-12`（休市/結算交割）、`2026-02-28`（休市，假日優先於週末，note=和平紀念日）等關鍵日期，對應規格 §8 全部 7 項測試案例；並額外測試閏年（2028 年 366 天）。
- **驗收指令**：三段指令實際執行結果與規格「預期」完全一致。
- **不要做的事**：未見連線外部網站（皆用 fixture / `MockTransport`）、未涉及資料庫、未解析 TDR/權證/特別股/ETN/興櫃（fixture 與測試明確排除 `030001`、`2881A`、`9105`）、未使用 pandas/lxml（僅 `bs4` + `html.parser`）、parser 內未做「臺→台」正規化（測試 `test_parse_name_with_symbols` 明確驗證 `臺企銀` 保留原字）。

### 發現問題

- **minor**：`etl/twstock_etl/dates.py:4` `from datetime import date, datetime` 匯入了未使用的 `datetime`，屬於死碼/lint 問題，不影響功能與測試，建議下個任務順手移除。
- 未發現 blocker 或 major 等級問題。

### 結論

**APPROVE**

規格狀態表已將 T0-2 更新為 `DONE` 並填入本審查報告路徑；因無 blocker/major，直接提交程式碼與規格狀態變更。
