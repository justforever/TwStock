# T1-4d 審查報告

規格：`docs/specs/M1-price.md` §T1-4d（`jobs.py`：`refresh_stock_list`／`refresh_trading_calendar`／`rebuild_calendar_from_index` 補 `etl_job_log`）。

## 第 1 輪（2026-09-19）

### 審查範圍

- `git diff` 未 commit 變更：`docs/specs/M1-price.md`（狀態表 TODO → IN_REVIEW）、`etl/twstock_etl/jobs.py`、新增 `etl/tests/test_etl_job_records.py`。
- 未動到規格明列「不准動」的 `load_daily_price`／`load_index_month`／`load_adj_factors`／`is_trading_day` 與三個 `*JobResult` dataclass。

### 自行驗證

1. 逐行比對 `jobs.py` 的 `refresh_stock_list`、新增的 `_write_calendar_year`、`refresh_trading_calendar`、`refresh_calendar_with_next_year`、`rebuild_calendar_from_index`，與規格 §T1-4d 第 2～6 節給出的程式碼**逐字相符**（含頂層 `from collections.abc import Sequence` 的位置）。
2. `etl/tests/test_etl_job_records.py` 與規格第 7 節給出的內容逐字相符。
3. 用 `scripts/pg_temp.sh start` 起臨時 PostgreSQL 16，執行：
   - `.venv/bin/python -m pytest etl/tests/test_etl_job_records.py -q` → `3 passed`。
   - `.venv/bin/python -m pytest etl/tests -q` → 全數 `80 passed`，無 failed（未跑到根目錄需要 TimescaleDB 的整合測試，範圍內無影響）。
4. 額外手動驗證（規格文字未逐行列出但屬合理邊界）：
   - **冪等性**：同一份 `isin_twse_strmode2.html`／`twse_holiday_schedule_2026.json` 對 `refresh_stock_list`／`refresh_trading_calendar` 各重跑兩次，`upsert` 結果完全一致（`records=9` 兩次相同、`CalendarLoadResult` 兩次相等），`etl_job_log` 正確各留兩列 `success`（每次真的執行都記一列，非去重欄位，符合 D-022／D-025 的設計：去重靠 `has_successful_run`，不是靠不寫 log）。
   - **失敗路徑**：`rebuild_calendar_from_index` 在指數交易日 < 200 時，確認 `SourceFormatError` 仍會往外拋，且 `etl_job_log` 留下 `('calendar_from_index', '2026', 'failed')` 一列，符合規格「指數資料不足是真的失敗」的要求。
   - **正常路徑不可誤記 failed（D-025 核心）**：`refresh_trading_calendar` 的 `build_calendar` 年份檢查在 `job_run` 之外，`refresh_calendar_with_next_year` 的 `build_calendar(year+1,...)` 也在 `try` 內、`_write_calendar_year` 呼叫在 `try` 外——確認程式結構與規格一致，「尚未公布」不會插入 `job_run` 的 `running` 列，自然也不會誤記 `failed`。
   - `refresh_stock_list` 的兩個停用保護錯誤訊息（`初次建庫下限`、`70%` 門檻文字）逐字未改，既有測試斷言仍成立。
5. `ruff check` / `flake8` 對 `jobs.py` 有 3 個既有 `F401`（`json`、`Path`、`StockRecord` 未使用），用 `git stash` 確認這 3 個問題在本次改動之前就存在，非本任務引入，不計入本輪缺失。

### 未逐項覆蓋、但不視為 blocker 的觀察

- `refresh_calendar_with_next_year` 本身雖因共用 `_write_calendar_year` 而一併獲得 `job_run` 覆蓋，但 `etl/tests/test_etl_job_records.py` 未新增其專屬測試——規格第 7 節明確只列 3 個測試，且該函式的 job_log／skip 行為已由 `etl/tests/test_etl_scheduler.py` 覆蓋（本輪已確認該檔存在且整體測試套件通過），不構成本任務缺失。

### 結論

**APPROVE**。改動與規格 §T1-4d 逐字相符，未逾越規格允許的兩個檔案範圍，未觸碰規格禁止修改的部分；自行重跑測試與手動驗證台股 ETL 冪等性、正常路徑不誤記 failed、失敗路徑正確記錄，均符合 D-025 的要求。
