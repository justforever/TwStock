# T1-4c 審查報告：`scheduler.py`：`_log_job_outcome` helper 與四個 wrapper 的 log 測試

## 第 1 輪

- 對應規格：`docs/specs/M1-price.md` §T1-4c
- 對應 commit：`99bf0f8`（訊息誤植為 `wip(T1-4d): 未通過審查，標記 BLOCKED`，實際內容是 T1-4c 的範圍；已於未經審查狀態下進入 `main`，本次為補審）
- 結論：**APPROVE**

### 審查方式

未採信任何回報，自行以 `git show 99bf0f8` 取得完整 diff（`etl/twstock_etl/scheduler.py`、`etl/tests/test_etl_scheduler.py`、`docs/specs/M1-price.md` 狀態表），並自行執行下列驗收指令：

```bash
cd /home/claude/TwStock && .venv/bin/python -m pytest etl/tests/test_etl_scheduler.py -q
# 實際輸出：8 passed

cd /home/claude/TwStock && grep -n "    from datetime import" etl/twstock_etl/scheduler.py
# 實際輸出：無（符合預期）

cd /home/claude/TwStock && TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) \
  .venv/bin/python -m pytest -rs -q
# 實際輸出：188 passed，1 skipped（db/tests/test_db_hypertable.py，因本機 PostgreSQL 16 無 TimescaleDB，規格容許此項 skip）
```

### 逐項檢查

1. **範圍**：commit 只動了 `etl/twstock_etl/scheduler.py`、`etl/tests/test_etl_scheduler.py` 與 `docs/specs/M1-price.md`（狀態表一行），未動 `jobs.py`／`cli.py`，符合「只准動這兩個檔案」的限制。
2. **頂層 import**：第 4 行改為 `from datetime import datetime, timedelta`，`run_adj_factors_job` 內原本的區域 `from datetime import timedelta` 已刪除；全檔 `grep "    from datetime import"` 無輸出，符合規格「不准再出現函式內的 from datetime import」。
3. **`_log_job_outcome` helper**：簽名、docstring、邏輯（`skip_reason is not None` → 記「略過：{reason}」；否則記「成功載入 {rows} 筆」）與規格逐字一致，插入位置也在 `TAIPEI = ...` 之後、`run_stock_list_job` 之前。
4. **三個 wrapper 改寫**（`run_daily_price_job`／`run_index_month_job`／`run_adj_factors_job`）：程式碼與規格範例逐字相同，`try/except Exception: logger.exception(...)` 的錯誤處理保持不變，未被 `_log_job_outcome` 吃掉例外（`_log_job_outcome` 在 `try` 區塊內被呼叫，若 `load_xxx` 拋例外仍會被外層 `except` 捕捉）。
5. **未動的部分**：`run_stock_list_job`、`run_trading_calendar_job` 函式本體、`build_scheduler`（含所有 cron 時間、`timezone`、`coalesce`／`max_instances`／`misfire_grace_time`、`next_run_time`）逐行比對規格前一版本，完全沒有變動，符合「不准動」的限制，也沒有把這兩者改成走 `_log_job_outcome`（它們的回傳型別本來就不是 `PriceJobResult` 系列 dataclass，維持原邏輯是對的）。
6. **測試**：
   - `test_run_index_month_job_success`、`test_run_adj_factors_job_success` 的斷言已依規格改為 `"TAIEX 指數 成功載入 3 筆"`、`"除權除息 成功載入 2 筆"`，並補上 `assert "Traceback" not in caplog.text`。
   - 新增 `test_run_daily_price_job_success`、`test_run_daily_price_job_skip`、`test_run_trading_calendar_job_success` 三個測試，內容與規格範例逐字相同，全部用 `monkeypatch` 換掉被呼叫的 job 函式，不連 DB、不打網路，斷言不依賴系統當前日期。
   - `test_scheduler_jobs`、`test_scheduler_job_triggers`、`test_run_stock_list_job_error_handling` 三個既有測試未被修改。
   - `test_run_daily_price_job_skip` 特別驗證「略過」不會被誤記成「成功載入 0 筆」，也不會產生任何 `ERROR` 等級的 log record，對應第 1 輪 Blocker 1 的回歸測試意圖，程式行為（`skip_reason is not None` 分支）與此一致。
7. **契約一致性**：確認 `jobs.py` 內 `PriceJobResult`／`IndexJobResult`／`AdjFactorJobResult` 三個 dataclass 都具備 `rows: int` 與 `skip_reason: str | None = None` 欄位，與 `_log_job_outcome` 的假設（docstring 所述「result 需有 rows: int 與 skip_reason: str | None」）相符，型別上不會在執行期出錯。
8. **台股資料細節（民國年、千分位、`--` 空值、股 vs 張、休市日、還原股價累乘方向、除權息當日）**：本次變更僅涉及 `scheduler.py` 的 log 輸出包裝與對應測試，未觸碰任何解析／換算／寫入邏輯（那些邏輯在 `jobs.py`、`sources/*.py`，本次未變動），故無這些細節的風險；已於前面 T1-1～T1-4b 各輪審查涵蓋，此處不重複檢查。
9. **ETL 冪等性**：本次未改動任何寫入 DB 的邏輯（`load_daily_price`／`load_index_month`／`load_adj_factors`／`etl_job_log` 寫入皆在 `jobs.py`，本 commit 未變動），冪等性不受影響。
10. **安全**：無新增硬編碼機密、無 SQL 語句異動。

### 附帶觀察（non-blocking）

- commit 訊息標題誤植為 `wip(T1-4d)` 且內容寫「未通過審查，標記 BLOCKED」，但實際 diff 是 T1-4c 的範圍且未曾送審；這是流程紀錄問題，已由後續 `039504d`／`4c30e04` 及 `docs/decisions.md` D-027 記錄與修正，不影響本次程式審查結論，故不列為 blocker。

### 結論

無 blocker、無 major 問題。程式與規格逐字相符，測試涵蓋成功／略過兩種路徑且驗證無誤記為 ERROR，全專案測試（188 passed / 1 skipped，skip 為預期中的 TimescaleDB 限制）皆通過。

**APPROVE**
