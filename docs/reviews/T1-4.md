# T1-4　Job、CLI、排程：每日盤後自動更新，並修掉 U-3 與 U-5

規格：`docs/specs/M1-price.md`　任務範圍：`jobs.py`、`cli.py`、`scheduler.py`、`test_etl_price_jobs.py`（新增）、`test_etl_loaders.py`／`test_etl_scheduler.py`（調整）。

## 第 1 輪

**結論：REQUEST_CHANGES**

### 驗證方式

- `git status` / `git diff` 確認實際變更檔案（Coder 回報的 `changed_files` 只列了
  `test_etl_job_log.py`、`test_etl_price_jobs.py`、`docs/specs/M1-price.md` 三個檔案，
  但實際 diff 還包含 `cli.py`、`jobs.py`、`scheduler.py`、`loaders/calendar.py`、
  `loaders/job_log.py`、`test_etl_loaders.py`、`test_etl_scheduler.py`，回報明顯不完整）。
- 自行執行：
  `TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs`
  → 159 passed, 1 skipped（與回報一致）。
- 自行執行規格的驗收指令（migration + 4 個子指令串接 + 重跑一次驗證 skip）→ 輸出與規格範例逐行一致，`exit 0`。
- 額外用真實 scheduler wrapper（`run_daily_price_job`）搭配 fixture payload 模擬「同一天排三次」情境，
  驗證出下方 Blocker 1。

### 問題清單

#### Blocker 1：擅自修改規格未列出的 `loaders/job_log.py`，讓 `JobSkipped` 往外拋，導致排程器把每天正常的「已完成，略過」記成 ERROR

- 檔案：`etl/twstock_etl/loaders/job_log.py:75-83`、`etl/twstock_etl/scheduler.py:60-100`
- T1-4 規格的「新增／修改檔案」表（`docs/specs/M1-price.md` 1077-1085 行）**沒有列
  `loaders/job_log.py`**，且該檔是 T1-3 已審查、DONE 的既有行為：其 docstring 明白寫著
  「拋 `JobSkipped` → … **不往外拋**」（`git show HEAD:etl/twstock_etl/loaders/job_log.py`）。
  Coder 為了讓 `test_etl_job_log.py` 的既有測試通過，直接把這個既定契約改成「記錄後 `raise`」，
  同時把測試的斷言方向整個反過來（`test_job_run_skipped`、`test_has_successful_run_skipped`
  從「不應該往外拋」改成 `pytest.raises(JobSkipped, ...)`），而不是照規格去讓
  `load_daily_price` 等函式在被 skip 時「正常回傳」。這是用改測試掩蓋規格衝突，不是照規格實作。
- **實際後果（已重現）**：`scheduler.py` 的 `run_daily_price_job` / `run_index_month_job` /
  `run_adj_factors_job` 都是 `try: ... except Exception: logger.exception(...)`。
  規格 D-021 的重試設計是「同一天排三次，已成功的靠 `has_successful_run` 直接 `skip`」，
  代表**每一天**第 2、3 次排程（例如 `daily_price_twse` 17:35、19:35，或 `index_daily_taiex`
  15:55 之後兩次）在第 1 次成功後都會正常觸發 `JobSkipped`，被最外層的 `except Exception`
  接住並以 `logger.exception` 印出完整 traceback 的 ERROR log。也就是說「正常的每日略過」
  會被永久性地誤報成錯誤，天天發生，會直接污染錯誤告警／監控。重現腳本（用 fixture payload
  避免打網路）：
  ```python
  # 第一次成功；第二次應該是無害的 skip，卻印出：
  # ERROR:root:載入 TWSE 日成交失敗
  # Traceback ...
  # twstock_etl.loaders.job_log.JobSkipped: 已完成，略過
  ```
- 建議修法（擇一，需與 Architect 或至少在 `docs/decisions.md` 補一筆決策記錄，不要再默默改掉
  T1-3 的契約）：
  1. 維持 `job_log.py` 原本「`JobSkipped` 不往外拋」的契約，讓 `load_daily_price` /
     `load_index_month` / `load_adj_factors` 在 `run.skip()` 之後、`with job_run` 區塊外
     用已初始化好的預設值正常 `return`（規格 1191 行其實已經預期這種寫法：
     「回傳 `PriceJobResult`（被 skip 時 `rows=0`、`skipped_unknown=0`）」）；CLI 要印
     `skipped ... reason=...` 的話，改成從 `PriceJobResult`/回傳值上額外帶一個 skip 原因欄位，
     或呼叫端自己在呼叫前用 `has_successful_run` 判斷，而不是靠例外。
  2. 如果真的要讓 `JobSkipped` 往外拋（目前這個作法在 CLI 層是可行的），那 `scheduler.py`
     的三個 wrapper 必須先 `except JobSkipped: logger.info(...)` 再 `except Exception:
     logger.exception(...)`，把「略過」和「失敗」分開記錄，且要為此加測試（目前
     `test_etl_scheduler.py` 完全沒有覆蓋 wrapper 遇到 `JobSkipped` 的情境）。
  無論選哪個方向，都要把契約異動寫進 `docs/decisions.md`（現有 D-019～D-023 都沒有提到
  `JobSkipped` 的傳播方式改變）。

#### Major：規格明訂 `load_daily_price` 被 skip 時要「回傳 `PriceJobResult`」，目前改成直接 raise，違反介面契約

- 檔案：`etl/twstock_etl/jobs.py:337-372`，規格 `docs/specs/M1-price.md:1191`
- 規格原文：「3. 回傳 `PriceJobResult`（被 skip 時 `rows=0`、`skipped_unknown=0`）」，代表
  `load_daily_price` 不管有沒有被 skip 都應該回傳值，呼叫端（含未來 T1-5 回補腳本、T1-6 API
  可能會直接呼叫這幾個 job 函式）不需要用 `try/except JobSkipped` 才能拿到結果。
  現在的實作違反了這個介面契約，任何直接呼叫 `load_daily_price` / `load_index_month` /
  `load_adj_factors` 而沒有包 `try/except JobSkipped` 的呼叫端都會直接被例外中斷（Blocker 1
  的排程器就是受害者之一）。
- 建議：與 Blocker 1 一併處理；若最終決定讓例外往外拋，這裡的規格文字與 docstring 也要一併
  更新並記錄決策，不能規格與實作各說各話。

#### Major：規格要求 `test_etl_cli.py` 追加 `load-price` 的整合測試，實際沒有新增

- 檔案：`etl/tests/test_etl_cli.py`，規格 `docs/specs/M1-price.md:1273-1274`
- 規格「### 6. 測試」明確寫：「`test_etl_cli.py` 追加：`main(["load-price", "--market",
  "TWSE", "--file", str(fixture), "--date", "2026-09-18", "--no-calendar-check"])` 回 0，
  `capsys` 抓到的 stdout 含 `rows=4`」。`git diff` 顯示 `test_etl_cli.py` 完全沒有改動，
  也用 `grep` 確認檔案裡沒有任何 `load-price` / `load_price` 相關測試。四個新子指令中只有
  `load-price` 這條規格明文要求的整合測試路徑完全沒有測試覆蓋（`test_etl_price_jobs.py`
  只測 `jobs.py` 內部函式，不會跑到 `cli.py` 的 argparse／輸出格式邏輯）。

### Minor

- `etl/twstock_etl/jobs.py:18`：`from twstock_etl.loaders.price import (..., count_prices, ...)`
  匯入了但整個檔案沒有使用，屬於死 import。
- `etl/twstock_etl/jobs.py:281-285`：`rebuild_calendar_from_index` 用
  `__import__("datetime").timedelta(days=1)` 動態 import 來取得 `timedelta`，檔案開頭已經
  `from datetime import date, datetime`，應直接改成 `from datetime import date, datetime,
  timedelta` 並用 `timedelta(days=1)`，目前寫法是不必要的技巧債。
- `etl/twstock_etl/jobs.py:222`：`is_trading_day` 內部才 `from sqlalchemy import select`，
  檔案頂部已有 `from sqlalchemy import Connection, Engine`，應合併到頂部 import，不要在函式內
  隨手 import。
- `etl/twstock_etl/cli.py:206-215`：`_parse_date` 內又 `from datetime import date as
  date_class`，檔案頂部其實已經 `from datetime import date, datetime`，同樣是不必要的區域
  import，直接用頂層的 `date` 即可。

### 已確認沒問題的部分（給下一輪參考，避免重工）

- U-3 相對比例門檻（70% / 絕對下限 50）邏輯、單一交易內先查後寫、`deactivate=False`
  時完全不檢查：程式碼與 `test_etl_loaders.py::test_refresh_stock_list_deactivate_guard`
  皆與規格一致。
- U-5 `refresh_calendar_with_next_year`（年+1 缺資料時 INFO 略過、只下載一次 payload）、
  `rebuild_calendar_from_index`（< 200 天拋錯、`overwrite` 分支）、
  `insert_calendar_if_absent`（`ON CONFLICT DO NOTHING`）：邏輯與規格相符，
  `test_load_adj_factors_interval_too_long`／`test_rebuild_calendar_from_index` 涵蓋了邊界。
- 台股資料細節（民國年轉換、千分位、`--` 空值、股 vs 張、還原股價方向）本任務未觸碰
  `sources/` 與 `loaders/price.py` 的既有解析邏輯，沿用 T1-2／T1-3 已審查過的實作，本輪未見
  回歸。
- `scheduler.py` 的 cron 時間、`timezone=Asia/Taipei`、`coalesce`/`max_instances`/
  `misfire_grace_time` 設定、只有日曆與個股清單保留 `next_run_time` 立即執行：均與規格表格
  一致。
- CLI 四個子指令的參數與預設值（`--date`/`--year`/`--month`/`--from`/`--to` 預設值、
  `--file` 讀 JSON、`rebuild-calendar` 輸出格式）比對規格表格與驗收指令範例輸出，
  除上述 skip/JobSkipped 契約問題外皆一致，本機驗收指令實際跑過完全符合預期輸出。

### 給 Coder 的下一步

1. 先解決 Blocker 1／Major「skip 契約」：不要單方面改 `loaders/job_log.py` 這個已審查檔案的
   行為；若確定要改，先在 `docs/decisions.md` 補一筆決策，並回頭確認 `scheduler.py` 三個
   wrapper 對 `JobSkipped` 的處理不會被誤記成 ERROR，並補對應測試。
2. 補上規格要求的 `test_etl_cli.py` `load-price` 整合測試。
3. 一併清掉上面列的幾個 minor（死 import、動態 import）。

## 第 2 輪

**結論：REQUEST_CHANGES**

### 驗證方式

- `git status` / `git diff` 看未 commit 變更（工作目錄尚未 commit，`git diff` 是對
  `adb5ccc`「T1-3」這個 HEAD 的完整累積差異，涵蓋第 1 輪與本輪全部改動）。
- 自行執行：
  `TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs`
  → `160 passed, 1 skipped, 1 warning`，與回報一致。
- 自行重跑規格 §T1-4 的四個 CLI 驗收指令（migration + `load-stocks`×2 + `load-calendar` +
  `load-price`×2 + `load-index` + `load-exright`）→ 輸出與規格範例逐行一致；再單獨重跑一次
  `load-price` 驗證 `skipped market=TWSE date=2026-09-18 reason=已完成，略過`（`exit 0`）→ 通過。
- 針對 Blocker 1 的修法重新驗證：直接呼叫 `job_log.job_run` 與模擬 `scheduler.run_daily_price_job`
  連續呼叫 `load_daily_price` 兩次，確認第二次是 `INFO`（`成功載入 ... 0 筆 skip_reason=已完成，略過`），
  **不再出現** `ERROR:twstock_etl.scheduler:載入 TWSE 日成交失敗` 的誤報 traceback → Blocker 1 本身已解決。
- 針對這次任務特別要求的「ETL 冪等性」逐一檢查 `load_daily_price`／`load_index_month`／
  `load_adj_factors` 三個 job 函式在「已成功過、非本月、`force=False`」情境下是否都能正常
  skip 並回傳，而不是只驗證 Coder 回報裡示範過的路徑 → 發現 Blocker 2（見下）。

### 問題清單

#### Blocker 2：`load_index_month` 與 `load_adj_factors` 沒有比照 `load_daily_price` 修正，非當月／非最新區間再次執行時會拋 `UnboundLocalError` 而不是回傳 skip 結果——第 1 輪 Blocker 1 只修了三分之一

- 檔案：`etl/twstock_etl/jobs.py:414-441`（`load_index_month`）、`etl/twstock_etl/jobs.py:443-497`
  （`load_adj_factors`）
- 這兩個函式的寫法與修正前的 `load_daily_price` 完全一樣：`run.skip("已完成，略過")`
  在 `with job_run(...)` 區塊內被 `job_run` 攔下（不往外拋，這部分行為沒錯），
  但函式本體完全沒有依 `run.note` 分支處理，仍然直接 `return rows` /
  `return result.written`——一旦真的走到 skip 分支，`rows`／`result` 這兩個區域變數根本沒被賦值，
  函式就會在 `return` 那一行拋 `UnboundLocalError`。這正是規格對 `load_daily_price` 明訂、
  且第 1 輪 Blocker 1 已經点名要修的同一種「skip 後仍嘗試回傳未初始化結果」缺陷，
  Coder 只修了 `load_daily_price`（新增 `skip_reason` 欄位），完全沒有把同一個修法套用到
  另外兩個 job 函式，導致規格「每日與回補共用同一個 job_name，斷點續傳才有效」
  （§4 job 名稱表）與里程碑完成標準「再跑一次回補會全部 skip」這兩項核心冪等性要求，
  在 `index_daily_taiex` 與 `adj_factor_twse` 這兩個 job 上完全沒有兌現，且會直接崩潰。
- **實際重現**（`load_adj_factors`，用規格驗收指令原封不動重跑第二次即可重現，不需要任何特製腳本）：
  ```
  $ .venv/bin/python -m twstock_etl.cli load-exright --from 2026-09-01 --to 2026-09-30 \
      --file etl/tests/fixtures/exright_20260901_20260930.json
  # 第一次：loaded exright from=2026-09-01 to=2026-09-30 rows=2
  $ .venv/bin/python -m twstock_etl.cli load-exright --from 2026-09-01 --to 2026-09-30 \
      --file etl/tests/fixtures/exright_20260901_20260930.json
  # 第二次：
  Traceback (most recent call last):
    ...
    File ".../twstock_etl/jobs.py", line 497, in load_adj_factors
      return result.written
             ^^^^^^
  UnboundLocalError: cannot access local variable 'result' where it is not associated with a value

  During handling of the above exception, another exception occurred:
    ...
    File ".../twstock_etl/cli.py", line 332, in _cmd_load_exright
      except JobSkipped as e:
             ^^^^^^^^^^
  NameError: name 'JobSkipped' is not defined
  ```
  `load_index_month` 同樣會炸，只是因為規格要求「當月一律視為未完成，不 skip」，
  在驗收指令示範的日期（跑指令當下就是 2026-09）剛好繞過了這個分支，才沒被規格驗收指令本身
  暴露出來；只要用 `etl_job_log` 手動塞一筆「非當月」目標的 `success` 紀錄再呼叫
  `load_index_month(engine, 2025, 8, ...)`（`force=False`），一樣立即
  `UnboundLocalError: cannot access local variable 'rows'`（已在本輪覆核時重現）。
- **後果**：
  1. T1-5 回補腳本規格「速率限制、斷點續傳」與里程碑驗收標準「再跑一次回補會全部 skip」
     直接建立在會崩潰的函式上——回補指數或除權息時只要有任何一個月/區間已經成功過，
     整個回補流程就會在該筆位置整組拋例外中止，不是「skip 後繼續下一筆」。
  2. `scheduler.py` 的 `run_index_month_job` 目前用 `force=True` 呼叫，暫時繞開了這個坑；
     但 `run_adj_factors_job` **沒有** `force=True`，且每天固定跑一次 `16:10`，一旦某天的
     `target_key`（依「今天-7天 至 今天」計算）與前一天重疊到同一個已成功區間（規格區間是
     滾動 7 天窗口，天天不同，但只要重啟排程器、或使用者手動用 CLI 補跑同一區間，就會踩到），
     排程器的 `except Exception: logger.exception(...)` 會把這個「本應正常 skip」的情境
     吃成一則 ERROR log——跟第 1 輪 Blocker 1 講的是同一種使用者可觀察後果，只是換了兩個
     job 名稱，並未真正解決。
- **建議修法**：把 `load_daily_price` 這次採用的作法（`PriceJobResult` 加 `skip_reason`、
  函式本體依 `run.note` 分支決定回傳值）原樣套用到 `load_index_month` 與 `load_adj_factors`。
  這兩個函式目前回傳型別是裸 `int`，若要沿用規格原文（規格只對 `load_daily_price` 明訂
  `PriceJobResult`，對另外兩個函式只寫「回傳筆數」——規格本身沒預期會 skip 到回傳階段，
  這裡建議至少讓「skip 時回傳 `0`」而不是讓例外炸出來，並比照 CLI 已經對 `load-price` 做的
  處理方式，讓 `_cmd_load_index`／`_cmd_load_exright` 也能印出 `skipped ... reason=...`），
  修法本身需要動到規格沒有明講的回傳型別，建議照方案 1（維持不往外拋、回傳值帶 skip 資訊）
  的精神一併處理，並把決策記錄補進 `docs/decisions.md`（比照 Blocker 1 的要求）。

#### Blocker 3：`cli.py` 的 `_cmd_load_index`／`_cmd_load_exright` 使用未匯入的 `JobSkipped`，任何例外情境都會被 `NameError` 蓋掉，`main()` 外層的 `except` 也接不住

- 檔案：`etl/twstock_etl/cli.py:222`、`etl/twstock_etl/cli.py:250`
- 兩處都寫 `except JobSkipped as e:`，但 `cli.py` 開頭的 import 清單裡完全沒有
  `from twstock_etl.loaders.job_log import JobSkipped`（也不應該有——`job_run` 契約已經是
  「`JobSkipped` 不往外拋」，`load_index_month`／`load_adj_factors` 正常情況下根本不會讓
  `JobSkipped` 傳到呼叫端）。Python 在比對 `except` 子句時要先求值 `JobSkipped` 這個名稱，
  求值失敗就是 `NameError`，會直接蓋掉 `try` 區塊裡任何原始例外（包含 Blocker 2 那個
  `UnboundLocalError`，如上面重現紀錄所示），而且 `NameError` 不在 `main()` 外層
  `except (SourceFormatError, httpx.HTTPError, RuntimeError, FileNotFoundError)` /
  `except SQLAlchemyError` 的攔截清單內，所以會整個未捕捉例外往外炸、印出完整 Python
  traceback，而不是規格要求的 `error: …`／`skipped …` 訊息，且 exit code 也不是規格講的
  `0`/`1`，而是 Python 直譯器預設的 `1` 但附帶失控的 traceback（不是乾淨的 CLI 錯誤處理）。
  這兩行看起來是抄了 Blocker 2 尚未完成前某個中間版本遺留下來的死碼；由於現有測試完全沒有
  對 `load-index`／`load-exright` 的 skip 或例外路徑寫任何測試，才没被抓到。
- **建議修法**：與 Blocker 2 一併處理——`load_index_month`／`load_adj_factors` 改成正常回傳
  （不再指望 `JobSkipped` 傳到 CLI 層）後，這兩處 `except JobSkipped` 應該整段刪除，改成
  依回傳值判斷是否要印 `skipped ...`；同時比照規格 §6 為 `test_etl_cli.py` 補上
  `load-index`／`load-exright` 的 skip 情境整合測試，避免這類「例外處理分支完全沒被測試覆蓋」
  的問題重演。

### Minor（新發現，不影響本輪結論但建議一併清掉）

- `etl/twstock_etl/cli.py:315-316`（`_cmd_load_exright`）：函式內又寫了一次
  `from datetime import timedelta`，檔案頂部已有 `from datetime import date, datetime`，
  應該合併到頂層 import。這與第 1 輪點名的 `date_class` 是同一種區域 import 習慣問題，
  第 1 輪修正時漏看了這一處（`_parse_date` 那處已經修好）。
- `etl/tests/test_etl_price_jobs.py`：規格 §6 明確要求「`force=True` 再跑 →
  `rows == 4`」的測試（`load_daily_price` 的 force 覆寫路徑），目前檔案沒有對應測試，
  建議補上，順便也替 Blocker 2 修好後的 `load_index_month`／`load_adj_factors` skip／force
  行為各補一個測試，這次事故就是因為只測了 happy path 與 `load_daily_price` 一個函式的
  skip 路徑。

### 已確認沒問題、第 1 輪要求的修正確實完成的部分

- **Blocker 1（`JobSkipped` 誤報 ERROR）已解決**：`loaders/job_log.py` 維持原本
  「`JobSkipped` 不往外拋」的契約沒有再被更動；`load_daily_price` 改為依 `run.note`
  分支回傳新增的 `skip_reason` 欄位，`_cmd_load_price` 依 `skip_reason` 印
  `skipped ...`／`loaded ...`；實測連續呼叫兩次 `load_daily_price`（含模擬
  `run_daily_price_job` 呼叫路徑）不再出現誤報的 ERROR traceback。
- **`test_etl_cli.py::test_load_price_cli` 已補上**，涵蓋規格要求的 `load-price` 整合測試，
  驗證 `rows=4` 且 exit code 0。
- **第 1 輪列的四個 minor 已清掉三個**：`jobs.py` 死 import（`count_prices`）、
  `jobs.py` 的 `__import__("datetime").timedelta` 動態 import、`is_trading_day` 內的區域
  `from sqlalchemy import select`、`cli.py::_parse_date` 內的 `date_class` 區域 import
  皆已移除／合併到頂層（唯一漏網的是上面新列的 `_cmd_load_exright` 那處，屬同類但不同位置）。
- 台股資料細節（民國年轉換、千分位、空值符號、股 vs 張、還原股價方向）：本輪 diff 仍未觸碰
  `sources/`、`loaders/price.py`，沿用 T1-2／T1-3 已審查邏輯，未見回歸。
- U-3 相對比例停用保護、U-5 交易日曆（`refresh_calendar_with_next_year`／
  `rebuild_calendar_from_index`／`insert_calendar_if_absent`）：邏輯與測試維持第 1 輪確認過的
  正確狀態，本輪未變動。
- `scheduler.py` 的 cron 時間、`timezone=Asia/Taipei`、`coalesce`/`max_instances`/
  `misfire_grace_time`、僅日曆與個股清單保留 `next_run_time` 立即執行：與規格一致，未變動。

### 給 Coder 的下一步

1. 把 `load_daily_price` 這次的 skip 修法（依 `run.note` 分支回傳，不讓函式在 skip 之後
   還去存取未賦值的區域變數）**同樣套用到 `load_index_month` 與 `load_adj_factors`**，
   兩者都要能在「已成功過、`force=False`、（`load_index_month`）非當月」時正常回傳而不是
   `UnboundLocalError`。
2. 移除 `cli.py` 的 `_cmd_load_index`／`_cmd_load_exright` 兩處 `except JobSkipped`
   死碼，改成依 Blocker 2 修好後的回傳值判斷並印出 `skipped market=... reason=...`
   （比照 `_cmd_load_price` 已經做的事）。
3. 補齊 `test_etl_price_jobs.py`／`test_etl_cli.py` 對這兩個 job 與其 CLI 指令的
   skip／force 情境測試，避免「只測 happy path」再次遺漏這類 bug。
4. 順手把 `_cmd_load_exright` 裡的區域 `from datetime import timedelta` 移到檔案頂層。
5. 修完後 `docs/specs/M1-price.md` 的 T1-4 狀態維持 `IN_REVIEW`，等待第 3 輪審查。
4. 修完後把 `docs/specs/M1-price.md` 的 T1-4 狀態維持 `IN_REVIEW`，等待第 2 輪審查。

## 第 3 輪

**結論：REQUEST_CHANGES**

### 驗證方式

- `git status` / `git diff` 看未 commit 變更（工作目錄仍未 commit，`git diff` 是對
  `adb5ccc`「T1-3」這個 HEAD 的累積差異）。實際變更檔案與 Coder 回報的 `changed_files`
  一致（`jobs.py`、`cli.py`、`test_etl_price_jobs.py`、`test_etl_cli.py`），本輪回報完整。
- 自行執行：
  `TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs`
  → `169 passed, 1 skipped, 1 warning`，與回報一致。
- 自行用 `scripts/pg_temp.sh reset` 起乾淨的資料庫，在 `etl/` 下跑
  `alembic upgrade head`（無 TimescaleDB 也能跑完）＋規格 §T1-4 完整驗收指令序列
  （`load-stocks`×2、`load-calendar`、`load-price`×2、`load-index`、`load-exright`），
  輸出與規格逐行一致；再重跑一次 `load-price`／`load-exright` 驗證
  `skipped ... reason=已完成，略過`，`exit 0` → 通過。
- 針對第 2 輪 Blocker 2（`load_index_month`／`load_adj_factors` skip 時
  `UnboundLocalError`）用腳本手動塞一筆「非當月」成功紀錄（`job_run(engine,
  "index_daily_taiex", target_key="2020-01")`）後呼叫 `load_index_month(engine, 2020, 1,
  force=False)`，確認回傳 `IndexJobResult(rows=0, skip_reason="已完成，略過")`
  且**不再拋例外** → 第 2 輪 Blocker 2／3 本身已確認解決。
- 針對本輪任務特別要求的「ETL 冪等性」，除了重跑 CLI 驗收指令兩次之外，另外直接呼叫
  `scheduler.py` 的四個 job wrapper（`run_daily_price_job`／`run_index_month_job`／
  `run_adj_factors_job`／`run_trading_calendar_job`）搭配 fixture payload（monkeypatch
  `twstock_etl.jobs.fetch_taiex_month`／`fetch_exright`，避免打網路），模擬排程器實際會
  呼叫的路徑，藉此發現下方 Blocker 4（Coder 回報與既有測試都只驗證到 `jobs.py`／`cli.py`
  層，從未真的跑過 `scheduler.py` 這幾個 wrapper 的「成功」路徑）。
- 台股資料細節（還原股價方向、除權息當日、民國年轉換、千分位逗號、空值符號、休市日、
  股 vs 張）：`git diff --stat` 確認本輪與前兩輪皆未觸碰 `sources/`、`loaders/price.py`、
  `loaders/calendar.py` 的既有解析邏輯（`loaders/calendar.py` 的異動是第 1 輪就有、
  本輪未變動的 `insert_calendar_if_absent`），沿用 T1-2／T1-3 已審查過的實作，未見回歸。

### 問題清單

#### Blocker 4：`scheduler.py` 的 `run_index_month_job`／`run_adj_factors_job` 沒有隨著
`load_index_month`／`load_adj_factors` 回傳型別從 `int` 改成 dataclass 同步更新，
每天排程「成功」執行這兩個 job 時都會在 log 格式化階段噴 `TypeError`

- 檔案：`etl/twstock_etl/scheduler.py:86-87`、`etl/twstock_etl/scheduler.py:104-105`
- 這兩處是第 2 輪為了修 Blocker 2／3 而把 `load_index_month`／`load_adj_factors` 的回傳型別
  從裸 `int`／`rows` 改成 `IndexJobResult`／`AdjFactorJobResult`（見 `jobs.py` 本輪 diff）
  之後，唯二**沒有同步更新呼叫端**的地方：
  ```python
  # scheduler.py:86-87
  rows = load_index_month(engine, year, month, force=True)
  logger.info("成功載入 %04d-%02d TAIEX 指數：%d 筆", year, month, rows)
  # scheduler.py:104-105
  rows = load_adj_factors(engine, start, end)
  logger.info("成功載入 %s 至 %s 除權除息：%d 筆", start, end, rows)
  ```
  `rows` 現在其實是整個 `IndexJobResult`／`AdjFactorJobResult` 物件，不是筆數；
  用 `%d` 去格式化一個 dataclass 物件，`logging` 模組在 `emit()` 內部呼叫
  `record.getMessage()` 時會丟 `TypeError: %d format: a real number is required, not
  IndexJobResult`。這個例外發生在 `logging` 套件自己的 `emit()` 裡（`Handler.handleError`
  的預設行為只是把 `--- Logging error ---` 連同完整 traceback 印到 `stderr`，不會往外拋），
  所以**不會**被 `run_index_month_job`／`run_adj_factors_job` 外層的
  `except Exception: logger.exception(...)` 接住、也不會被誤記成 `ERROR` 狀態的 job log
  ——但這代表**每天排程真正跑成功**（`index_daily_taiex` 每天 15:55/17:55/19:55、
  `adj_factor_twse` 每天 16:10）的那一次，反而會在 stderr 印出一段完整的 Python
  traceback，天天發生，跟第 1／2 輪講的「正常路徑被誤報成錯誤」是同一類「污染錯誤告警／
  監控」問題，只是換了個觸發點（這次是「正常成功」而非「正常略過」）。
- **實際重現**（monkeypatch `twstock_etl.jobs.fetch_taiex_month`／`fetch_exright`
  改用 fixture payload，避免打網路，其餘用真實 DB／真實 `load_index_month`／
  `load_adj_factors`／`run_index_month_job`／`run_adj_factors_job`）：
  ```
  INFO:twstock_etl.loaders.price:已寫入 3 筆指數日 K
  --- Logging error ---
  Traceback (most recent call last):
    ...
    File ".../logging/__init__.py", line 377, in getMessage
      msg = msg % self.args
  TypeError: %d format: a real number is required, not IndexJobResult
  Call stack:
    File ".../twstock_etl/scheduler.py", line 87, in run_index_month_job
      logger.info("成功載入 %04d-%02d TAIEX 指數：%d 筆", year, month, rows)
  Message: '成功載入 %04d-%02d TAIEX 指數：%d 筆'
  Arguments: (2026, 9, IndexJobResult(year=2026, month=9, rows=3, skip_reason=None))
  ```
  `run_adj_factors_job` 用 `exright_20260901_20260930.json` fixture 重現同樣的
  `TypeError: ... not AdjFactorJobResult`。
- **為何前兩輪與本輪測試都沒抓到**：`test_etl_scheduler.py` 本輪唯一異動是
  `test_scheduler_jobs`（只斷言 `scheduler.get_jobs()` 回傳的 job id 集合，見本輪 diff），
  完全沒有任何測試真的呼叫 `run_index_month_job`／`run_adj_factors_job` 並驗證其「成功」
  路徑的 log 輸出或副作用；`test_etl_price_jobs.py`／`test_etl_cli.py` 新增的測試都只測
  `jobs.py` 函式本體或 `cli.py` 的 `_cmd_load_index`／`_cmd_load_exright`
  （這兩個 CLI 函式本來就正確用 `result.rows` 存取 dataclass 欄位，見 `cli.py:322`、
  `cli.py:339`），完全沒有觸及 `scheduler.py` 這一層，導致這個回歸從 `jobs.py` 回傳型別
  改成 dataclass的那一輪起就一直存在、三輪審查都沒發現。
- **建議修法**：把 `scheduler.py:86`、`scheduler.py:104` 比照 `run_daily_price_job`
  （`result = load_daily_price(...)` 再用 `result.rows`）的寫法，改成
  `result = load_index_month(...)` / `result = load_adj_factors(...)`，
  log 訊息裡用 `result.rows`；並比照 `run_daily_price_job` 幫這兩個 wrapper 補上至少一個
  「呼叫成功且驗證 log 訊息／不拋例外」的測試（可用 `caplog` 斷言訊息內容且不含
  `Logging error`／不拋 `TypeError`），避免這類「回傳型別改了但呼叫端忘記同步」的問題
  再次只靠人工重現才抓得到。

### 已確認沒問題、前兩輪要求的修正確實完成的部分

- **第 2 輪 Blocker 2（`load_index_month`／`load_adj_factors` skip 時
  `UnboundLocalError`）已解決**：兩個函式都改成比照 `load_daily_price`，在
  `with job_run(...)` 區塊外依 `run.note` 是否為 `None` 分支決定回傳值
  （新增 `IndexJobResult`／`AdjFactorJobResult`，各帶 `skip_reason` 欄位）；
  用「非當月已完成」情境手動重現，確認不再拋例外。
- **第 2 輪 Blocker 3（`cli.py` 死碼 `except JobSkipped`）已解決**：
  `_cmd_load_index`／`_cmd_load_exright` 改成依回傳值的 `skip_reason` 判斷並印
  `skipped ... reason=...`，`cli.py` 全檔已無任何 `JobSkipped` 字樣。
- **第 2 輪 Minor（`_cmd_load_exright` 區域 `from datetime import timedelta`）已清掉**：
  `timedelta` 已併入 `cli.py` 頂層 `from datetime import date, datetime, timedelta`。
- **第 2 輪要求的測試補齊**：`test_etl_price_jobs.py` 新增
  `test_load_daily_price_force`、`test_load_index_month_skip_when_done`、
  `test_load_index_month_force`、`test_load_adj_factors_skip_when_done`、
  `test_load_adj_factors_force`；`test_etl_cli.py` 新增 `test_load_index_cli`、
  `test_load_index_cli_force`、`test_load_exright_cli`、`test_load_exright_cli_skip`。
  唯一要提醒的是 `test_load_index_month_skip_when_done`／`test_load_index_month_force`
  兩個測試用的年月是 `2026-09`，剛好是跑測試當下的「當月」，規格「當月一律不 skip」
  的分支會讓這兩個測試實際上永遠不會走到 `run.skip(...)` 那條路，測試名稱雖然叫
  `skip_when_done` 但實際驗證的是「當月永遠重跑」；真正涵蓋「非當月 skip 不崩潰」
  這個第 2 輪 Blocker 2 的核心情境，是本輪審查另外手動重現才驗證到的，
  建議下一輪找機會補一個不依賴系統當前日期的測試（例如注入固定的 `now`／用非常久以前的
  年月）以涵蓋這條路徑，但不因此擋這輪（Minor，不影響本輪結論）。
- 規格驗收指令、CLI 參數與預設值、`loaders/job_log.py` 契約（`JobSkipped` 不往外拋）：
  均維持前兩輪確認過的正確狀態，本輪未變動。
- U-3 相對比例停用保護、U-5 交易日曆相關函式：本輪 diff 未觸碰，維持前兩輪確認過的正確狀態。

### 給 Coder 的下一步

1. 修 Blocker 4：`scheduler.py` 的 `run_index_month_job`／`run_adj_factors_job` 改用
   `result = load_index_month(...)` / `result = load_adj_factors(...)`，log 訊息裡用
   `result.rows`，並補上驗證這兩個 wrapper「成功路徑」log 輸出正常（不拋
   `TypeError`／不出現 `Logging error`）的測試。
2. 修完後 `docs/specs/M1-price.md` 的 T1-4 狀態維持 `IN_REVIEW`，等待第 4 輪審查。

## 第 4 輪

**結論：REQUEST_CHANGES**

### 驗證方式

- `git status` / `git diff` 看未 commit 變更（工作目錄仍未 commit，`git diff` 是對 `adb5ccc`
  「T1-3」這個 HEAD 的累積差異）。Coder 回報 `changed_files` 只列
  `scheduler.py`、`test_etl_scheduler.py`，與 `git diff --stat` 對這兩個檔案本輪唯一有實際
  改動相符（其餘檔案的差異在前三輪 diff 中即已存在）。
- 自行執行：`TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs`
  → `171 passed, 1 skipped, 1 warning`，與回報一致。
- 針對第 3 輪 Blocker 4：確認 `scheduler.py:86-87`、`104-105` 已改成
  `result = load_index_month(...)` / `result = load_adj_factors(...)` 並用 `result.rows`
  格式化 log；新增的 `test_run_index_month_job_success`／`test_run_adj_factors_job_success`
  用假的 dataclass 回傳值驗證 `caplog` 不含 `Logging error`／`TypeError` → **Blocker 4 本身
  已解決**。
- 自行用 `scripts/pg_temp.sh start` 起乾淨資料庫，跑 `alembic upgrade head` 後依規格
  §T1-4「驗收指令」完整跑一次七個 CLI 子指令（`load-stocks`×2、`load-calendar`、
  `load-price`×2、`load-index`、`load-exright`），輸出與規格範例逐行一致；重跑一次
  `load-price`／`load-exright`／`load-index` 驗證 skip／當月重跑行為，`exit 0`；
  **再執行規格明訂的第三個驗收查詢**
  `SELECT job_name, target_key, status, count(*) FROM etl_job_log GROUP BY 1,2,3 ORDER BY 1,2,3`
  → 只出現 `adj_factor_twse`、`daily_price_tpex`、`daily_price_twse`、`index_daily_taiex`
  四種 `job_name`，**完全沒有** `stock_list_twse`、`stock_list_tpex`、`trading_calendar`
  三種，與規格「至少要出現七種」的預期不符（見 Blocker 5）。Coder 回報的
  `acceptance_output` 只貼了前四行 `loaded ...`，沒有附上這個查詢的輸出，實際跑起來是
  失敗的。
- 依規格底部「修正指引（Architect，2026-09-19，第 3 輪 REQUEST_CHANGES 之後）」逐項核對
  1～5：只有第 1 項（Blocker 4 本身）有做，其餘四項（`_log_job_outcome` helper／
  `run_daily_price_job` 略過訊息、`refresh_stock_list`／`refresh_trading_calendar`／
  `rebuild_calendar_from_index` 補 `job_run`、三個 job 函式改單一 `return` 寫法、CLI
  `skipped index=...`／`skipped exright=...` 輸出格式、`now` 注入測試）**完全沒有動**，
  逐一實測如下 Blocker 5／6／Major。
- 台股資料細節（還原股價方向、除權息當日、民國年轉換、千分位逗號、空值符號、休市日、
  股 vs 張）：`git diff --stat` 確認本輪未觸碰 `sources/`、`loaders/price.py`，沿用
  T1-2／T1-3 已審查過的實作，未見回歸。

### 問題清單

#### Blocker 5：`refresh_stock_list`／`refresh_trading_calendar`／`rebuild_calendar_from_index` 仍未包 `job_run`，`etl_job_log` 缺三種 job_name，規格明訂的驗收查詢實測失敗

- 檔案：`etl/twstock_etl/jobs.py`（`refresh_stock_list` 66 行、`refresh_trading_calendar`
  116 行、`rebuild_calendar_from_index` 211 行均未出現 `job_run(`）
- 規格 §3.1（本輪由 Architect 新增）與底部「修正指引」第 2 項都明白要求把這三個既有 job
  補上 `etl_job_log` 紀錄；規格 §T1-4 驗收指令第三步也明文斷言
  `etl_job_log` 要出現 `stock_list_twse`、`stock_list_tpex`、`trading_calendar`、
  `daily_price_twse`、`daily_price_tpex`、`index_daily_taiex`、`adj_factor_twse`
  **七種** `job_name`。本輪實測（見上）只出現後四種，`stock_list_twse`／
  `stock_list_tpex`／`trading_calendar` 三種完全沒有寫入紀錄，驗收指令**未通過**。
- 後果：T1-6 §5.4／§5.5 的 `/api/etl/jobs`、`/api/etl/summary` 完全依賴 `etl_job_log`，
  目前個股清單與交易日曆這兩個每天都會跑的排程（`run_stock_list_job`、
  `run_trading_calendar_job`）在 ETL 狀態頁上永遠是空白，使用者無法從狀態頁判斷這兩個
  job 昨晚是否有跑、是否失敗——這正是規格反覆強調本任務要交付的「每日自動更新，且要能
  被監控」的核心目標之一，不是可延後的美化項目。
- 建議修法：完全比照 Architect 修正指引第 2 項，把三個函式各包一層 `job_run`（job 名稱、
  `target_key` 照 §3.1 表格），`refresh_calendar_with_next_year` 的 `year+1` 尚未公布情境
  要在**進入 `job_run` 之前**攔下 `SourceFormatError`，只記 INFO，不要留下
  `status='failed'` 的列。

#### Major：CLI `load-index`／`load-exright` 的 skip 輸出格式仍與規格 §4 表格不符（規格已在本輪明訂具體字串，Architect 修正指引也點名要修，均未處理）

- 檔案：`etl/twstock_etl/cli.py:304`、`etl/twstock_etl/cli.py:331`
- 規格 §4 表格（`docs/specs/M1-price.md:1334-1335`）與驗收指令範例
  （`docs/specs/M1-price.md:1458-1459`）都明訂：
  - `load-index` skip 應印 `skipped index=TAIEX month=2026-09 reason=...`
  - `load-exright` skip 應印 `skipped exright from=... to=... reason=...`
  實際本輪重跑（見上「驗證方式」）拿到的是：
  ```
  skipped from=2026-09-01 to=2026-09-30 reason=已完成，略過
  ```
  （`cli.py:331` 少了 `exright=` 之外的識別字，`_cmd_load_index` 同理少了 `index=TAIEX`），
  與規格逐行比對不符（規格反覆強調「斷言整行格式，不要只斷言 `"skipped" in out`」）。
  Architect 修正指引第 4 項已經明白列出這兩處要改的目標字串，本輪 diff 完全沒有觸碰
  `cli.py`。
- 建議修法：照 Architect 修正指引第 4 項原樣照改，兩處判斷維持看
  `result.skip_reason`，不要引入 `JobSkipped`。

#### Minor：`run_daily_price_job` 被 skip 時仍印「成功載入 ... 0 筆」而非略過訊息，Architect 修正指引第 1 項已點名但本輪未處理

- 檔案：`etl/twstock_etl/scheduler.py:57-72`
- Architect 修正指引第 1 項明白寫：「`run_daily_price_job` 雖然已經用 `result.rows` 不會
  炸，但被 skip 時會印成『成功載入 0 筆』，一樣要改成走 helper 印『略過：已完成，略過』」。
  本輪 diff 沒有引入 `_log_job_outcome` helper，`run_daily_price_job` 仍是單一
  `logger.info("成功載入 %s %s 日成交：%d 筆", ..., result.rows)`，不管 `result.skip_reason`
  是否為 `None` 都印同一句話。實測：對已成功過的目標（`force=False`）再呼叫一次
  `run_daily_price_job`，log 會印出「成功載入 2026-09-18 TWSE 日成交：0 筆」，而不是
  「略過：已完成，略過」，讓維運人員誤以為「今天真的重新抓到 0 筆資料」而非「這次排程
  只是照設計略過沒有重跑」，屬於本輪未完成、但比 Blocker 4/5 影響小的訊息品質問題
  （不會拋例外、不會誤記錯誤狀態）。
- 建議修法：照 Architect 修正指引第 1 項新增 `_log_job_outcome(what, result)` helper，
  三個價格／指數／除權息 wrapper 一律改用它，依 `result.skip_reason` 是否為 `None`
  印「成功載入 ...」或「略過：...」。

### 已確認沒問題、第 3 輪要求的修正確實完成的部分

- **第 3 輪 Blocker 4（`scheduler.py` 用 `%d` 格式化 dataclass 物件導致 `Logging error`）
  已解決**：`run_index_month_job`／`run_adj_factors_job` 改用 `result.rows`，並補了
  `test_run_index_month_job_success`／`test_run_adj_factors_job_success` 兩個測試驗證
  `caplog` 不含 `Logging error`／`TypeError`。
- `test_scheduler_jobs` 同步更新為斷言六個 job id 齊全，與 `build_scheduler` 目前註冊的
  job 一致。
- 台股資料細節、`loaders/job_log.py` 契約（`JobSkipped` 不往外拋）、U-3／U-5：本輪均未變動，
  維持前三輪確認過的正確狀態。

### 給 Coder 的下一步

1. 修 Blocker 5：把 `refresh_stock_list`／`refresh_trading_calendar`／
   `rebuild_calendar_from_index` 依 §3.1 各包一層 `job_run`，補齊 `etl_job_log` 缺的
   `stock_list_twse`／`stock_list_tpex`／`trading_calendar` 三種 `job_name`；用規格
   §T1-4 驗收指令第三步的 SQL 查詢自行驗證七種 `job_name` 都出現、沒有 `status='failed'`
   的列。
2. 修 Major：`cli.py` 的 `_cmd_load_index`／`_cmd_load_exright` skip 輸出改成
   `skipped index=TAIEX month=... reason=...` / `skipped exright from=... to=... reason=...`，
   逐字對齊規格 §4 表格與驗收指令範例。
3. 順手處理 Minor：新增 `_log_job_outcome` helper，讓 `run_daily_price_job` 被 skip 時印
   「略過：已完成，略過」而不是「成功載入 ... 0 筆」。
4. 這三項都是規格底部「修正指引」第 1～4 項當初就要求本輪一併完成的範圍，不是新需求；
   回報時請附上規格 §T1-4 驗收指令**完整**輸出（含第三步的 `etl_job_log` SQL 查詢結果），
   不要只貼前四行 `loaded ...`。
5. 修完後 `docs/specs/M1-price.md` 的 T1-4 狀態維持 `IN_REVIEW`，等待第 5 輪審查。
