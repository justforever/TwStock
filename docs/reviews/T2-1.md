# T2-1 審查報告：技術債（回補收進 CLI U-9、`daily_price` 索引 U-14、備份腳本 U-12）

規格：`docs/specs/M2-chips.md` §「T2-1　技術債：回補收進 CLI（U-9）、`daily_price` 索引（U-14）、備份腳本（U-12）」

## 第 1 輪

- 審查者：Reviewer
- 日期：2026-09-21
- 結論：**APPROVE**

### 變更範圍檢查

`git status` / `git diff` 顯示變更檔案為：

- 修改：`etl/Dockerfile`、`etl/twstock_etl/cli.py`、`scripts/backfill.py`、`docs/specs/M2-chips.md`（狀態表 T2-1 改 `IN_REVIEW`，這是 Coder 唯一可改本文件之處）
- 新增：`db/migrations/versions/0003_daily_price_index.py`、`db/tests/test_db_migration_0003.py`、`etl/tests/test_etl_backfill_cli.py`、`scripts/backup.sh`

與規格「只准動上面 7 個檔案」完全一致，未動 `etl/twstock_etl/backfill.py` 邏輯、未動 `scripts/m1_verify.sh`、未動 `deploy/docker-compose.yml`、未建籌碼表、未升級任何套件。

### 逐項檢查

1. **Migration `0003`**：`revision="0003"`、`down_revision="0002"`；`upgrade()` 建 `ix_daily_price_stock_date_desc (stock_id, trade_date DESC)`，`downgrade()` 對稱刪除。與規格範例逐字一致。
2. **`cli.py` backfill 子指令**：`backfill` 底下四個子子指令 `price` / `index` / `exright` / `calendar`，選項、`dest`、型別（`_parse_date` for price/exright，字串 for index，`int` for calendar 年份）與共用選項工廠 `_add_backfill_common_options`（`--sleep` float 預設 3.0、`--force`、`--source-dir` Path、`--dry-run`、`--max-failures` int 預設 10）皆與規格相符。`BACKFILL_EPILOG` 把 `scripts/backfill.py xxx` 改寫為 `python -m twstock_etl.cli backfill xxx`。`_cmd_backfill` 用 `get_engine()`（未自行讀環境變數）、依 `backfill_command` 分派、回傳碼語意（0 / 130 / 1 / 2）與規格一致；`BackfillAborted`／`KeyboardInterrupt` 例外處理位置正確，未在 `main()` 既有 `except` 之外多加一層。import 放在模組頂層。
3. **`etl/Dockerfile`**：`COPY etl /app/etl` 之後加 `COPY scripts /app/scripts`，`.dockerignore` 未動（規格允許）。
4. **`scripts/backfill.py`**：整支改為薄包裝，无殘留 argparse 定義，`sys.exit(main(["backfill", *sys.argv[1:]]))`，與規格逐字一致。
5. **`scripts/backup.sh`**：`set -euo pipefail`；`DATABASE_URL` 未設定即印錯誤退出 1；把 `postgresql+psycopg://` 換成 `postgresql://`；`OUT_DIR` 預設 `data/backup`；檔名格式正確；`pg_dump` 找不到時退回 `/usr/lib/postgresql/16/bin/pg_dump`，再找不到才報錯；只保留最新 7 份；成功印 `BACKUP OK <路徑> <大小>`；檔頭有還原指令註解。已 `chmod +x`。
6. **測試**：`etl/tests/test_etl_backfill_cli.py` 五個案例（正常呼叫帶對參數、失敗回 1、中斷回 130、超過上限回 2、index 年月是字串）與規格逐一對應，皆用 `monkeypatch` 隔離 DB。`db/tests/test_db_migration_0003.py` 用 `clean_db` 查 `pg_indexes` 驗證索引存在且含 `trade_date DESC`。

### 自行執行的驗收指令（不採信回報，全部重跑）

```
TWSTOCK_TEST_DATABASE_URL=$(scripts/pg_temp.sh start) .venv/bin/python -m pytest -rs
→ 222 passed, 1 skipped（M1 的 216 + 本任務新增 6；skip 為既有 TimescaleDB 未裝那筆）

.venv/bin/alembic -c db/alembic.ini upgrade head / downgrade 0002 / upgrade head
→ 最後一行 REVERSIBLE_OK

.venv/bin/python -m twstock_etl.cli backfill --help
→ 出現 "usage: ... backfill" 與 price/index/exright/calendar 四個子指令

scripts/m1_verify.sh
→ 最後一行 M1 VERIFY PASSED（薄包裝未改變行為）

scripts/backup.sh /tmp/twstock-backup-test
→ BACKUP OK /tmp/twstock-backup-test/twstock_20260921_035940.dump 16K

grep -c 'COPY scripts /app/scripts' etl/Dockerfile → 1

docker compose -f deploy/docker-compose.yml --env-file .env.example config --quiet
→ COMPOSE_OK
```

全部符合規格「驗收指令」章節的預期結果。

### 台股資料細節 / ETL 冪等性

本任務明確聲明「不碰任何籌碼邏輯」，也未改 `backfill.py` 內部的抓取/清洗/寫入邏輯，只是把既有的回補邏輯換一個 CLI 入口，因此民國年轉換、千分位、`--` 空值、股/張單位、休市日、`ON CONFLICT` 等冪等性行為維持 M1 原狀，未受影響；`m1_verify.sh`（含斷點續傳 resume check）通過即為佐證。

### 安全

無硬編碼機密；`backup.sh` 的 DB 連線字串來自環境變數並直接傳給 `pg_dump`（無使用者輸入拼接 SQL 的風險）；CLI 新增的參數皆走 `argparse`，無字串拼接組指令。

### 問題清單

無 blocker / major / minor。

### 結論

**APPROVE**。狀態表 T2-1 改為 `DONE`。
